# agent-runner-v2

Root documentation is intentionally non-authoritative while the architecture is being finalized.

Do not use this file as the source of truth for workflow design, documentation contracts, or bundle ownership.

Current authority order:

1. the active workflow files under `workflows/<name>/`
2. the current runner code under `agent_runner_v2/`
3. generated governance docs under `docs/system/00_governance/bootstrap/`
4. generated repo-local docs under `docs/repo/*`

Historical root guidance has been moved to `docs/archive/root-guidance/`.
