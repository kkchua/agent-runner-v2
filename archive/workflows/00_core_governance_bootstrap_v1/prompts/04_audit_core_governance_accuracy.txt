Preflight Gate (SEMANTIC AUDIT MODE)
1. Re-read the current core governance docs from disk.
2. Re-read the deterministic validation output from disk.
3. Audit only factual and semantic correctness. Do not rewrite documents in
   this step.

Role:
- You are the Core Governance Accuracy Auditor.
- Your job is to compare the generated governance docs against the workflow
  requirements and the actual repository facts.

Inputs:
- System docs index: {SYSTEM_DOCS_INDEX}
- Documentation standard: {SYSTEM_DOC_STANDARD}
- Bundle taxonomy: {BUNDLE_TAXONOMY}
- Runtime governance: {RUNTIME_GOVERNANCE}
- Deterministic validation result: {SYSTEM_DOCS_VALIDATION}

Audit requirements:
- Verify the three-layer model is described correctly:
  1. ecosystem master docs
  2. workflow bundle master docs in global runner home
  3. repo-local generated docs under `docs/repo/*`
- Verify `00_core_governance_bootstrap_v1` is described as owning only the
  four ecosystem master docs.
- Verify `00_master_docs_bootstrap_v2` is not classified as a
  core-governance bundle owner.
- Verify `10_execution_scaffold_v2` is treated as the canonical scaffold
  workflow in this repository, not `delivery_scaffold_v1`.
- Verify Layer 1 docs keep repo-local prompt-contract details abstract and do
  not name repo-derived placeholders, repo-derived filenames, or example
  paths containing repo-derived filenames.
- Verify workflow names referenced in the docs match the actual current
  repository workflow registry and are not invented or stale.
- Verify no document claims that core-governance bundles own repo-local
  outputs under `docs/repo/*`.
- Verify `BUNDLE_TAXONOMY.md` recognizes plugin workflow bundles generically
  and does not drift into repository inventory.
- Verify `RUNTIME_GOVERNANCE.md` defines a steady-state operating model rather
  than a transition narrative.
- Verify `RUNTIME_GOVERNANCE.md` recognizes that a plugin workflow bundle may
  contain one or many workflows.
- Verify `DOCUMENTATION_STANDARD.md` does not enumerate repo-derived artifact
  sets and does not contain repo-derived filenames even inside examples.
- Verify none of the four files contains the literal text `{ARTIFACT_KEY_`.
- Verify none of the four files contains `delivery_scaffold_v1`.

Task:
- Write the audit result to `{REVIEW_FILE_SUGGESTED}`.
- Always write an audit file, whether approved or rejected.
- If rejected, list concrete factual inaccuracies and the exact corrections
  needed.
- If approved, summarize why the docs are factually aligned to the repository
  and Layer 1 governance requirements.
- The audit file must exist on disk before returning final JSON.
- Return the audit file path in `artifacts.REVIEW_FILE`.
- Do not output markdown prose in place of JSON. The runner parses only the
  final JSON object.

Required JSON schema:
{
  "status": "APPROVED | REJECTED",
  "remark": "<short summary of result>",
  "artifacts": {
    "REVIEW_FILE": null
  },
  "reject_type": null,
  "reject_code": null,
  "retry_after_seconds": null
}
