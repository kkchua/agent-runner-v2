You are refining the Layer 1 core governance docs after review feedback.

Read first:
- `{SYSTEM_DOCS_INDEX}`
- `{SYSTEM_DOC_STANDARD}`
- `{BUNDLE_TAXONOMY}`
- `{RUNTIME_GOVERNANCE}`
- `{REVIEW_FILE_SUGGESTED}`

Task:
- update only the four governance files owned by this workflow
- resolve every review issue directly
- preserve the core ownership split:
  - global governance docs
  - workflow bundle master docs
  - repo-local generated docs under `docs/repo/*`
- preserve these factual rules while refining:
  - `10_execution_scaffold_v2` is the canonical scaffold workflow in this
    repository
  - Layer 1 governance docs must keep repo-local prompt-contract details
    generic and must not name repo-derived placeholders
  - `00_master_docs_bootstrap_v2` must not be classified as a
    core-governance bundle
  - core governance bundles do not own repo-local outputs under
    `docs/repo/*`
  - plugin workflow bundles are first-class runtime objects and may contain
    one or many workflows

Enforce these scope rules while refining:
- remove any repository-specific workflow inventory from `README.md`
- keep `DOCUMENTATION_STANDARD.md` at contract/rules level only
- keep `BUNDLE_TAXONOMY.md` at bundle-class and ownership-boundary level
- keep `RUNTIME_GOVERNANCE.md` as a steady-state operating model, not a
  migration narrative
- remove any occurrence of `00_master_docs_bootstrap_v2` from
  `BUNDLE_TAXONOMY.md`
- remove any occurrence of `delivery_scaffold_v1`
- remove any literal `{ARTIFACT_KEY_*}` guidance
- remove any repo-derived artifact name, repo-derived filename, or repo-local
  example path from `DOCUMENTATION_STANDARD.md`
- remove any hardcoded SDLC workflow list or concrete plugin inventory from
  Layer 1 docs
- when a sentence is not essential to Layer 1 governance scope, delete it
  instead of broadening it into a tutorial

Document-specific refine rules:
- `README.md`: only `00_core_governance_bootstrap_v1` and optionally
  `10_execution_scaffold_v2` may be named concretely
- `DOCUMENTATION_STANDARD.md`: define standards only for the four Layer 1
  docs
- `BUNDLE_TAXONOMY.md`: define bundle classes generically, not a repo
  inventory
- `RUNTIME_GOVERNANCE.md`: define `_registry`, publish/install, role and
  connection resolution, artifact ownership, execution parity, and generic
  plugin-bundle control expectations

Before returning final JSON, re-read the four files and confirm:
- `DOCUMENTATION_STANDARD.md` has no repo-derived artifact names,
  repo-derived filenames, or repo-local example paths that embed
  repo-derived filenames
- `BUNDLE_TAXONOMY.md` does not classify `00_master_docs_bootstrap_v2`
- `RUNTIME_GOVERNANCE.md` recognizes multi-workflow plugin bundles
- none of the four files has `{ARTIFACT_KEY_`
- none of the four files has `delivery_scaffold_v1`
- state explicitly in your remark what you removed from
  `DOCUMENTATION_STANDARD.md` and whether any repo-local examples remain
