---
template_id: "SYS-03-BL"
version: "1.0.0"
doc_type: "workflow_output"
authority: "workflow-generated"
scan_policy: "include"
scan_reason: "backlog for initiative execution"
layer: "layer3"
platform: "agent-runner-v2"
lifecycle_status: "draft"
effective_version: "20260807-002"
managed_by: "workflow-generated"
---

# Backlog: Incremental Codebase Documentation Updates with Git Hook Automation

## Document Metadata

- Document ID: BACKLOG-20260807-001
- Source plan reference: PLAN-20260806-001_incremental-codebase-doc-update.md
- Source requirement reference: REQ-20260806-001_incremental-codebase-doc-update.md
- Date of generation: 2026-08-07
- Producing workflow: sdlc_30_backlog_v1
- Producing agent: Workflow Architect

## Backlog Overview

This backlog decomposes the approved plan (PLAN-20260806-001) into ordered work items for the initiative: Incremental Codebase Documentation Updates with Git Hook Automation.

The initiative delivers three major capabilities:

1. An action-only workflow package (update_codebase_docs_v1) with four sequential steps for incremental documentation regeneration. This is generated by workflow_builder_v1 from the existing spec.
2. Two CLI subcommands (install-codebase-hook, uninstall-codebase-hook) for git hook lifecycle management.
3. A post-commit git hook shell script that detects relevant source code changes and triggers the workflow via backend submission.

Total work items: 8

Effort summary: Total relative effort is estimated at 21 story points across all work items. The majority of effort is concentrated in the CLI hook command module (WI-02, 5 SP) and the hook script template (WI-03, 3 SP).

Ordering rationale: Work items are ordered to build foundational components first (workflow package, hook script template), then integration layer (CLI commands, dispatch), then validation (integration testing, performance). Dependencies flow strictly forward.

## Plan Traceability

This backlog traces directly to PLAN-20260806-001 and its three architectural components:

| Plan Component | Work Items | Plan Section |
|---|---|---|
| Component 1: update_codebase_docs_v1 workflow | WI-01 | Component Breakdown, Component 1 |
| Component 2: CLI Hook Commands | WI-02, WI-04 | Component Breakdown, Component 2 |
| Component 3: Post-Commit Hook Script | WI-03 | Component Breakdown, Component 3 |
| IP-005: Concurrency Check | WI-05 | Integration Points, IP-005 |
| IP-001: Hook to Backend via CLI | WI-06 | Integration Points, IP-001 |
| OQ-003: build_snapshot() performance | WI-07 | Open Questions, OQ-003 |
| End-to-end validation | WI-08 | Prerequisites, Data Flow |

### Requirement Coverage

All 18 functional requirements (FR-001 through FR-018) and all 10 non-functional requirements (NFR-001 through NFR-010) from REQ-20260806-001 are addressed through the work items in this backlog. Traceability mapping:

| Requirement | Work Items |
|---|---|
| FR-001 (Incremental workflow) | WI-01 |
| FR-002 (Changed file detection) | WI-01 |
| FR-003 (Selective regeneration) | WI-01 |
| FR-004 (Install CLI) | WI-02 |
| FR-005 (Uninstall CLI) | WI-02 |
| FR-006 (CLI targeting) | WI-02 |
| FR-007 (Post-commit hook) | WI-03 |
| FR-008 (Backend submission) | WI-03, WI-06 |
| FR-009 (Tracking file) | WI-01 |
| FR-010 (Auto-commit) | WI-01 |
| FR-011 (Missing tracking file) | WI-01 |
| FR-012 (No relevant changes) | WI-01 |
| FR-013 (Unreachable commit) | WI-01 |
| FR-014 (Missing doc structure) | WI-02, WI-04 |
| FR-015 (Hook logging) | WI-03 |
| FR-016 (Concurrency prevention) | WI-05 |
| FR-017 (Auto-commit failure) | WI-01 |
| FR-018 (Manifest update) | WI-01 |
| NFR-001 (Lightweight hook) | WI-03 |
| NFR-002 (CLI idempotency) | WI-02 |
| NFR-003 (Concurrency safety) | WI-05 |
| NFR-004 (Function reuse) | WI-01 |
| NFR-005 (Action step compliance) | WI-01 |
| NFR-006 (Standard sidecar) | WI-01 |
| NFR-007 (Hook packaging) | WI-03 |
| NFR-008 (Performance) | WI-07 |
| NFR-009 (Repo-agnostic) | WI-01, WI-02 |
| NFR-010 (CLI pattern) | WI-02, WI-04 |

### Affected Codebase Areas

| Work Item | Affected Modules (from codebase_manifest.json) |
|---|---|
| WI-01 | workflows/update_codebase_docs_v1/ (new package) |
| WI-02 | agent-runner-v2-codebase-hook-commands (new module: codebase_hook_commands.py) |
| WI-03 | agent-runner-v2-data-hooks (new file: data/hooks/post-commit) |
| WI-04 | agent-runner-v2-run-agent (modified: run_agent.py dispatch) |
| WI-05 | agent-runner-v2-submit-commands or new CLI subcommand |
| WI-06 | Integration across all components |
| WI-07 | agent-runner-v2-codebase-docs (performance measurement) |
| WI-08 | End-to-end pipeline validation |

## Work Items

### WI-20260807-002_incremental-codebase-doc-update-01

**Title:** Generate update_codebase_docs_v1 Workflow Package

**Description:** Process the workflow spec at docs/repo/workflow_builder/specs/incremental-codebase-update.md through workflow_builder_v1 to generate the update_codebase_docs_v1 workflow package. The package includes workflow.toml (manifest) and actions.py (package-local action functions for detect_changes, scan_affected_modules, regenerate_docs, commit_and_track steps). This is work stream 1 as defined in the plan, handled by a separate workflow (workflow_builder_v1).

**Parent plan component:** Component 1 (update_codebase_docs_v1 Workflow)

**Priority:** critical

**Estimated effort:** 3 story points

**Dependencies:** None (prerequisite for WI-06, WI-08)

**Required skills:** Workflow package system, action step model, @action() decorator pattern

**Requirements covered:** FR-001, FR-002, FR-003, FR-009, FR-010, FR-011, FR-012, FR-013, FR-017, FR-018, NFR-004, NFR-005, NFR-006, NFR-009

**Audit/compliance sensitivity:** Low. New workflow package follows established Layer 2 action step model.

**Assumptions:** The workflow spec at docs/repo/workflow_builder/specs/incremental-codebase-update.md is complete and accurate (ASSUMPTION-002). workflow_builder_v1 will produce both workflow.toml and actions.py conforming to the Layer 2 runtime model.

---

### WI-20260807-002_incremental-codebase-doc-update-02

**Title:** Implement codebase_hook_commands.py CLI Module

**Description:** Create a new module agent_runner_v2/codebase_hook_commands.py following the codebase_init_commands.py pattern. The module provides a main() function with argparse-based argument parsing for two subcommands: install-codebase-hook and uninstall-codebase-hook. Both subcommands support default current-directory behavior and an explicit --repo /path/to/repo flag. The install subcommand validates the standard docs/repo/codebase/current/ structure, copies the hook script from package data to .git/hooks/, and marks the hook with a recognizable identifier for safe uninstall. The uninstall subcommand removes the hook script only if it is the agent-runner hook, preserving .last_sync_commit. Both commands must be idempotent (NFR-002).

**Parent plan component:** Component 2 (CLI Hook Commands)

**Priority:** critical

**Estimated effort:** 5 story points

**Dependencies:** WI-03 (hook script template must exist before install can copy it)

**Required skills:** CLI command design, argparse, file operations, git hook management, idempotent operations

**Requirements covered:** FR-004, FR-005, FR-006, FR-014, NFR-002, NFR-007, NFR-009, NFR-010

**Affected source files:**
- New: agent_runner_v2/codebase_hook_commands.py
- Pattern reference: agent_runner_v2/codebase_init_commands.py

**Audit/compliance sensitivity:** Medium. Modifies git repository state (hook installation). Idempotency and safe uninstall are critical to avoid data loss.

**Assumptions:** The codebase_init_commands.py pattern is stable and can be followed (DEP-008). The .git/hooks/ directory is writable by the user.

---

### WI-20260807-002_incremental-codebase-doc-update-03

**Title:** Create Post-Commit Hook Script Template

**Description:** Create the post-commit hook script template at agent_runner_v2/data/hooks/post-commit. The script must: (1) get the list of changed files in the current commit via git diff-tree, (2) filter to relevant file types (*.py, workflow.toml, pyproject.toml, requirements.txt, constants.py), (3) exit 0 silently if no relevant changes, (4) check for active runs of update_codebase_docs_v1 (via CLI helper), (5) skip and log if active run exists, (6) submit workflow via ukbe-run-agent submit --workflow-name update_codebase_docs_v1, (7) log result to .git/codebase-hook.log, (8) emit console warning on failure. The hook must always exit 0 to avoid blocking the commit. The hook must identify itself with a comment header for safe uninstall. The script must use POSIX-compatible commands and work under Git Bash on Windows, as well as natively on Linux and macOS.

**Parent plan component:** Component 3 (Post-Commit Hook Script)

**Priority:** critical

**Estimated effort:** 3 story points

**Dependencies:** None (prerequisite for WI-02, WI-06)

**Required skills:** Shell scripting (bash/POSIX), git internals (diff-tree, hooks), CLI invocation

**Requirements covered:** FR-007, FR-008, FR-015, NFR-001, NFR-007

**Affected source files:**
- New: agent_runner_v2/data/hooks/post-commit

**Audit/compliance sensitivity:** Low. Script is packaged data, not executed at build time.

**Assumptions:** Git for Windows includes a bash-compatible shell for hooks (OQ-004). The ukbe-run-agent CLI is available on PATH or via python -m fallback (per CLI Path Resolution Strategy in plan Component 3).

---

### WI-20260807-002_incremental-codebase-doc-update-04

**Title:** Add CLI Dispatch Entries to run_agent.py

**Description:** Add if-blocks to the run_agent.py parse_args() function for install-codebase-hook and uninstall-codebase-hook subcommands, following the established if/elif dispatch pattern used by codebase-init. Each subcommand captures raw arguments and delegates to codebase_hook_commands.py main(). This follows the pattern at run_agent.py:498-501 (codebase-init) and run_agent.py:691-693 (codebase-init dispatch).

**Parent plan component:** Component 2 (CLI Hook Commands) - dispatch integration

**Priority:** high

**Estimated effort:** 1 story point

**Dependencies:** WI-02 (codebase_hook_commands.py must exist before dispatch can import it)

**Required skills:** CLI dispatch pattern in run_agent.py

**Requirements covered:** FR-004, FR-005, FR-006, NFR-010

**Affected source files:**
- Modified: agent_runner_v2/run_agent.py (add if-blocks for new commands)

**Audit/compliance sensitivity:** Low. Follows established pattern (NFR-010).

**Assumptions:** The run_agent.py if/elif dispatch pattern remains the convention for CLI subcommands (DEP-006). This is a known exception to the registry pattern used elsewhere (per AGENTS.md).

---

### WI-20260807-002_incremental-codebase-doc-update-05

**Title:** Implement Concurrency Check CLI Helper

**Description:** Resolve open question OQ-001 by implementing a mechanism for the hook script to check for active runs of update_codebase_docs_v1 before submitting. Two options are presented in the plan: (a) a new CLI subcommand (e.g., ukbe-run-agent check-active-run --workflow-name update_codebase_docs_v1) that wraps BackendClient.list_runs() with workflow_name and status_group filters, or (b) relying on the backend to reject duplicate submissions. The backlog resolves this in favor of option (a) for explicit control and logging. The implementation uses BackendClient.list_runs() (agent_runner_v2/v2/backend_client_v1.py:103 and agent_runner_v2/v2/backend_client.py:123) with appropriate filters. The output format must be parseable by the hook script.

**Parent plan component:** Integration Points IP-005

**Priority:** high

**Estimated effort:** 2 story points

**Dependencies:** None

**Required skills:** BackendClient API, CLI subcommand design, JSON output formatting

**Requirements covered:** FR-016, NFR-003

**Affected source files:**
- New or extended: CLI subcommand module (pattern from submit_commands.py)
- API dependency: agent_runner_v2/v2/backend_client_v1.py list_runs()

**Audit/compliance sensitivity:** Medium. Concurrency safety is critical for preventing duplicate workflow executions (RISK-002).

**Assumptions:** BackendClient.list_runs() supports filtering by workflow_name and status_group (DEP-010). The backend can be queried from a shell script via CLI.

---

### WI-20260807-002_incremental-codebase-doc-update-06

**Title:** End-to-End Integration Validation

**Description:** Validate the complete trigger-execute pipeline: post-commit hook triggers on relevant commit, checks concurrency, submits workflow via CLI, backend queues the run, daemon claims and executes the workflow steps, documentation is regenerated and committed. This validates all integration points (IP-001 through IP-006) working together. Test scenarios include: (a) normal commit with relevant changes triggers full pipeline, (b) commit with no relevant changes exits silently, (c) concurrent hook trigger skips submission, (d) daemon not running produces logged failure, (e) auto-commit succeeds and .last_sync_commit is updated, (f) auto-commit failure leaves uncommitted changes with warning.

**Parent plan component:** Cross-component integration (Data Flow sections)

**Priority:** high

**Estimated effort:** 3 story points

**Dependencies:** WI-01, WI-02, WI-03, WI-04, WI-05 (all components must be functional)

**Required skills:** Integration testing, git hook testing, backend/daemon interaction, end-to-end debugging

**Requirements covered:** FR-001, FR-007, FR-008, FR-010, FR-015, FR-016, FR-017, NFR-001, NFR-003

**Audit/compliance sensitivity:** Medium. Validates the full automated pipeline that modifies repository state.

**Assumptions:** The daemon is available for testing. The backend accepts the update_codebase_docs_v1 workflow name (ASSUMPTION-001).

---

### WI-20260807-002_incremental-codebase-doc-update-07

**Title:** Performance Validation and build_snapshot() Analysis

**Description:** Resolve open question OQ-003 by measuring actual performance of the incremental workflow against the NFR-008 target (less than 50 percent of full-scan time for single-file changes). The plan documents a known limitation: build_snapshot() (codebase_docs.py:762) performs a full repository scan regardless of mode, so snapshot construction is O(all files). The performance benefit comes solely from selective render_module_doc() calls. This work item requires: (a) benchmarking full-scan execution time on the current codebase (141+ modules), (b) benchmarking incremental execution time for 1-file and 5-file changes, (c) comparing results against the NFR-008 target, (d) if the target is not met, evaluating options: (i) accept as known limitation, (ii) investigate lighter snapshot builder for incremental mode, (iii) investigate snapshot caching. Results should be documented for task-phase decision-making.

**Parent plan component:** Risk Assessment RISK-006, Open Question OQ-003

**Priority:** medium

**Estimated effort:** 2 story points

**Dependencies:** WI-01 (workflow package must exist for benchmarking)

**Required skills:** Performance profiling, benchmarking methodology, codebase_docs.py internals

**Requirements covered:** NFR-008

**Affected source files:**
- Analysis target: agent_runner_v2/codebase_docs.py (build_snapshot at line 762)

**Audit/compliance sensitivity:** Low. Measurement and analysis only.

**Assumptions:** The current agent-runner-v2 repository (141+ modules in codebase_manifest.json) is representative of the target deployment size.

---

### WI-20260807-002_incremental-codebase-doc-update-08

**Title:** Cross-Platform Hook Compatibility Validation

**Description:** Resolve open question OQ-004 by validating that the post-commit hook script works correctly on Windows (Git Bash), Linux, and macOS. This includes verifying: (a) POSIX-compatible commands work under Git for Windows bash, (b) the CLI path resolution strategy (direct PATH invocation, python -m fallback, optional install-time embedding) functions on all platforms, (c) file path separators are handled correctly in the hook script, (d) the .git/codebase-hook.log file is written correctly on all platforms. If platform-specific variants are needed, the install command must detect the OS and install the appropriate script.

**Parent plan component:** Component 3 (Post-Commit Hook Script) - portability risk

**Priority:** medium

**Estimated effort:** 2 story points

**Dependencies:** WI-03 (hook script template must exist), WI-02 (install command must support platform detection if needed)

**Required skills:** Cross-platform shell scripting, Windows Git Bash, CI/CD platform testing

**Requirements covered:** FR-007, NFR-001, NFR-009

**Affected source files:**
- agent_runner_v2/data/hooks/post-commit (may need platform-specific variants)
- agent_runner_v2/codebase_hook_commands.py (may need platform detection logic)

**Audit/compliance sensitivity:** Low.

**Assumptions:** Git for Windows includes a bash-compatible shell that supports the hook script (plan assumes this).

## Prioritization

### Priority Ordering

| Priority | Work Items | Rationale |
|---|---|---|
| Critical | WI-01, WI-02, WI-03 | These are the three core deliverables. Without them, no functionality exists. |
| High | WI-04, WI-05, WI-06 | Integration and dispatch layer. Required for end-to-end operation. |
| Medium | WI-07, WI-08 | Validation and performance. Important for quality but not blocking basic functionality. |

### Execution Sequence

The recommended execution order respects dependencies:

1. WI-01 (workflow package) and WI-03 (hook script template) can proceed in parallel - they have no mutual dependencies.
2. WI-02 (CLI module) depends on WI-03 (needs the hook script to copy).
3. WI-04 (dispatch) depends on WI-02 (needs the module to import).
4. WI-05 (concurrency check) is independent and can proceed in parallel with WI-02/WI-04.
5. WI-06 (integration) requires WI-01, WI-02, WI-03, WI-04, WI-05.
6. WI-07 (performance) requires WI-01.
7. WI-08 (cross-platform) requires WI-02, WI-03.

### Critical Path

The critical path is: WI-03 -> WI-02 -> WI-04 -> WI-06

WI-03 (hook script) is the earliest prerequisite. Without it, WI-02 cannot copy the script, WI-04 cannot dispatch to the install command, and WI-06 cannot test the full pipeline.

### Parallel Opportunities

- WI-01 and WI-03 can be developed in parallel (work stream 1 and work stream 3 from the plan).
- WI-05 can be developed in parallel with WI-02 and WI-04.
- WI-07 and WI-08 can be developed in parallel after their dependencies are met.

## Dependencies

### Inter-Item Dependencies

| Dependent Item | Depends On | Dependency Type | Description |
|---|---|---|---|
| WI-02 | WI-03 | Hard | install-codebase-hook copies the hook script from package data; the script must exist |
| WI-04 | WI-02 | Hard | Dispatch imports codebase_hook_commands.py; the module must exist |
| WI-05 | (none) | None | Independent; uses existing BackendClient.list_runs() |
| WI-06 | WI-01, WI-02, WI-03, WI-04, WI-05 | Hard | Integration requires all components functional |
| WI-07 | WI-01 | Hard | Performance benchmarking requires the workflow package |
| WI-08 | WI-02, WI-03 | Hard | Cross-platform validation requires hook script and install command |

### External Dependencies (from Plan)

| ID | Dependency | Status | Impact on Backlog |
|---|---|---|---|
| DEP-001 | codebase_docs.py rendering functions | Verified present | WI-01 reuses build_snapshot(), render_module_doc(), render_inventory(), render_change_impact() |
| DEP-002 | BackendClient.submit_run() and list_runs() | Verified present | WI-05 uses list_runs(); WI-01 uses submit_run() via CLI |
| DEP-003 | Daemon execution infrastructure | Verified present | WI-06 requires daemon for integration testing |
| DEP-006 | run_agent.py if/elif dispatch pattern | Verified present | WI-04 follows this pattern |
| DEP-007 | ACTION_REGISTRY dispatch in runner_actions.py | Verified present | WI-01 workflow package uses @action() decorator (package-local) |
| DEP-008 | codebase_init_commands.py CLI pattern | Verified present | WI-02 follows this pattern |
| DEP-009 | sync_codebase_docs.py action pattern | Verified present | WI-01 action steps follow this pattern |
| DEP-010 | BackendClient.list_runs() filtering | Verified present | WI-05 uses this API |

### Prerequisites

1. The workflow spec at docs/repo/workflow_builder/specs/incremental-codebase-update.md must be processed through workflow_builder_v1 to generate the update_codebase_docs_v1 package before WI-06 can be tested.
2. The target test repository must have a populated docs/repo/codebase/current/ directory (created by codebase-init or sdlc_00_codebase_v1).
3. The daemon must be running for hook-triggered workflow submissions to be processed during integration testing.
4. The backend must accept the update_codebase_docs_v1 workflow name for submission (ASSUMPTION-001).

## Acceptance Criteria

### WI-20260807-002_incremental-codebase-doc-update-01

- The update_codebase_docs_v1 workflow package exists with valid workflow.toml and actions.py.
- All four action functions (detect_changes, scan_affected_modules, regenerate_docs, commit_and_track) are registered via @action() decorator (package-local).
- The workflow executes successfully in any agent-runner-v2 enabled repository with the standard structure.
- No LLM calls are made during execution.
- Action steps reuse build_snapshot(), render_module_doc(), render_inventory(), render_change_impact() from codebase_docs.py.

### WI-20260807-002_incremental-codebase-doc-update-02

- install-codebase-hook installs a working post-commit hook in a repository with the standard structure.
- install-codebase-hook produces a clear error in a repository without the standard structure.
- uninstall-codebase-hook removes only the agent-runner hook and preserves .last_sync_commit.
- Both commands are idempotent (multiple runs produce no errors or side effects).
- Both commands support default current-directory and --repo flag targeting.

### WI-20260807-002_incremental-codebase-doc-update-03

- The hook script detects relevant file changes (*.py, workflow.toml, pyproject.toml, requirements.txt, constants.py).
- The hook script exits 0 silently when no relevant changes are present.
- The hook script submits the workflow via CLI command.
- The hook script logs results to .git/codebase-hook.log.
- The hook script always exits 0 to avoid blocking the commit.
- The hook script includes a recognizable identifier comment for safe uninstall.
- Local hook processing completes within 2 seconds (excluding network latency).

### WI-20260807-002_incremental-codebase-doc-update-04

- run_agent.py dispatches install-codebase-hook to codebase_hook_commands.py main().
- run_agent.py dispatches uninstall-codebase-hook to codebase_hook_commands.py main().
- The dispatch follows the established if/elif pattern (consistent with codebase-init).

### WI-20260807-002_incremental-codebase-doc-update-05

- The concurrency check CLI helper queries BackendClient.list_runs() with workflow_name and status_group filters.
- When an active run exists, the output indicates an active run is present.
- When no active runs exist, the output indicates no active runs.
- The output format is parseable by the hook script.

### WI-20260807-002_incremental-codebase-doc-update-06

- A commit with relevant changes triggers the full pipeline (hook -> submit -> daemon -> workflow -> commit).
- A commit with no relevant changes exits silently (no workflow submission).
- A concurrent hook trigger skips submission when an active run exists.
- Daemon not running produces a logged failure with console warning.
- Successful workflow execution results in committed documentation and updated .last_sync_commit.
- Auto-commit failure leaves uncommitted changes with a warning message.

### WI-20260807-002_incremental-codebase-doc-update-07

- Full-scan execution time is measured on the current codebase (141+ modules).
- Incremental execution time is measured for 1-file and 5-file changes.
- Results are compared against the NFR-008 target (less than 50 percent of full-scan time for single-file changes).
- If the target is not met, options are documented with recommendations.

### WI-20260807-002_incremental-codebase-doc-update-08

- The hook script works correctly under Git for Windows (bash).
- The hook script works correctly on Linux and macOS.
- The CLI path resolution strategy functions on all platforms.
- If platform-specific variants are needed, they are documented and the install command handles them.

## Effort Estimates

### Summary Table

| Work Item | Title | Effort (SP) | Priority |
|---|---|---|---|
| WI-20260807-002_incremental-codebase-doc-update-01 | Generate update_codebase_docs_v1 Workflow Package | 3 | critical |
| WI-20260807-002_incremental-codebase-doc-update-02 | Implement codebase_hook_commands.py CLI Module | 5 | critical |
| WI-20260807-002_incremental-codebase-doc-update-03 | Create Post-Commit Hook Script Template | 3 | critical |
| WI-20260807-002_incremental-codebase-doc-update-04 | Add CLI Dispatch Entries to run_agent.py | 1 | high |
| WI-20260807-002_incremental-codebase-doc-update-05 | Implement Concurrency Check CLI Helper | 2 | high |
| WI-20260807-002_incremental-codebase-doc-update-06 | End-to-End Integration Validation | 3 | high |
| WI-20260807-002_incremental-codebase-doc-update-07 | Performance Validation and build_snapshot() Analysis | 2 | medium |
| WI-20260807-002_incremental-codebase-doc-update-08 | Cross-Platform Hook Compatibility Validation | 2 | medium |
| **Total** | | **21** | |

### Effort Breakdown by Work Package

| Work Package | Work Items | Effort (SP) |
|---|---|---|
| Workflow Package Generation | WI-01 | 3 |
| CLI Commands and Hook Infrastructure | WI-02, WI-03, WI-04, WI-05 | 11 |
| Validation and Testing | WI-06, WI-07, WI-08 | 7 |

### Confidence Level

- High confidence: WI-01, WI-02, WI-03, WI-04 (well-specified in plan with verified dependencies)
- Medium confidence: WI-05, WI-06 (depends on backend behavior and integration complexity)
- Lower confidence: WI-07, WI-08 (exploratory; actual effort depends on findings)

### Effort Estimation Basis

Estimates are based on:
- Verified API availability in the current codebase (all dependencies confirmed present).
- Established patterns to follow (codebase_init_commands.py for CLI, sync_codebase_docs.py for action steps).
- Known complexity of shell script portability (WI-03, WI-08).
- Known performance uncertainty around build_snapshot() full scan (WI-07).

## Open Questions

### OQ-BL-001: Concurrency Check Implementation Approach (from plan OQ-001)

The plan presents two options for the hook concurrency check: (a) a dedicated CLI subcommand wrapping BackendClient.list_runs(), or (b) relying on the backend to reject duplicate submissions. This backlog resolves in favor of option (a) (captured in WI-05) for explicit control and logging. However, the exact CLI subcommand name and output format should be finalized during task decomposition.

**Status:** Resolved (option a selected). Implementation details deferred to task phase.

### OQ-BL-002: commit_and_track Action Implementation Strategy (from plan OQ-002)

The plan notes that the existing commit_changes action in sdlc_shared_actions.py already stages all of docs/repo/codebase/ and commits. It could be extended to also write .last_sync_commit before staging, or a new action function specific to this workflow may be cleaner. The workflow spec defines the required behavior. The backlog captures this as a task-phase decision within WI-01.

**Status:** Unresolved. Both options are viable. Task decomposition should evaluate based on code complexity and test coverage impact.

### OQ-BL-003: build_snapshot() Full Scan Performance Impact (from plan OQ-003)

The build_snapshot() function always performs a full repository scan regardless of mode. The performance benefit of incremental updates comes solely from selective render_module_doc() calls. Whether the NFR-008 target can be met depends on the ratio of snapshot construction time to total execution time. WI-07 is specifically allocated to measure and resolve this.

**Status:** Unresolved. Requires measurement (WI-07). Options: accept as limitation, lighter snapshot builder, snapshot caching.

### OQ-BL-004: Windows Hook Script Compatibility (from plan OQ-004)

The plan assumes bash-compatible shell script works under Git for Windows. If platform-specific variants are needed, the install command must detect the OS. WI-08 is allocated to validate this assumption.

**Status:** Unresolved. Requires testing (WI-08). Default assumption is that Git Bash is sufficient.

### OQ-BL-005: Backend Workflow Registration for update_codebase_docs_v1 (from plan ASSUMPTION-001 and RISK-005)

The plan assumes the backend accepts the update_codebase_docs_v1 workflow name without pre-registration. If this assumption fails, backend registration becomes a prerequisite that must be resolved before integration testing (WI-06) can succeed. This is outside the initiative scope but must be verified early.

**Status:** Unresolved. Should be verified as early as possible (ideally before WI-06). If backend registration is required, it becomes an external dependency.
