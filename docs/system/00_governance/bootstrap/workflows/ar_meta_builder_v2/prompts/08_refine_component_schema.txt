# Objective

Address review findings for the component schema. Fix specific issues
while preserving content that passed review.

# Reference Inputs

- REVIEW_COMPONENT_SCHEMA_FILE: {REVIEW_COMPONENT_SCHEMA_FILE}
- DOMAIN_COMPONENT_SCHEMA_FILE: {DOMAIN_COMPONENT_SCHEMA_FILE}

# Tasks

1. Read each review finding
2. Fix the identified issues
3. Preserve unchanged content
4. Update self-validation if structure changed

# Forbidden Content

- Do NOT add content beyond what review requires
- Do NOT include non-ASCII characters

# Output Instructions

Write updated schema to: {DOMAIN_COMPONENT_SCHEMA_FILE}
ASCII-only. Preserve YAML frontmatter.
