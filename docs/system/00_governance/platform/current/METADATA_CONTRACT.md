---
template_id: SYS-02-MC
version: "1.0"
doc_type: "platform_standard"
authority: "platform-owned"
scan_policy: "include"
scan_reason: "permanent Layer 2 metadata contract; defines platform-specific metadata extensions"
layer: "layer2"
platform: "agent-runner-v2"
lifecycle_status: "published"
effective_version: "02PC-20260720-86359b88"
managed_by: workflow-generated
---

> Managed by workflow: `02_platform_core_foundation_v1` / step: `publish_platform_core_set`
> This file is workflow-generated and protected from manual edits.

# Metadata Contract

## Purpose

This document defines the platform-specific metadata extensions for
agent-runner-v2. It extends the Layer 1 Metadata Standard with values
and fields specific to this platform's needs.

Layer 1 defines the baseline metadata fields and vocabularies. This
contract adds platform-specific `doc_type` values, `authority` values,
additional frontmatter fields, inheritance rules, and scan-policy
expectations.

All Layer 3 bundles on this platform must comply with both the Layer 1
baseline and this platform extension.

## Platform doc_type Values

The Layer 1 Metadata Standard defines the baseline `doc_type`
vocabulary. This platform extends it with the following values for
platform-specific document classification.

### Platform-Standard Values

| Value | Meaning | Layer |
|---|---|---|
| `platform_standard` | A Layer 2 operating standard for agent-runner-v2. Permanent platform constitution document. | Layer 2 |

### Bundle Definition Values

| Value | Meaning | Layer |
|---|---|---|
| `bundle_definition` | A bundle-local definition or control document owned by a specific Layer 3 workflow bundle. | Layer 3 |

### Inherited Values (from Layer 1)

The following values are inherited from Layer 1 and must not be
redefined:

| Value | Meaning |
|---|---|
| `masterplan` | Human-authored reference blueprint. |
| `system` | System-level governance or platform document. |
| `workflow_output` | Concrete output generated by a workflow execution. |
| `review_artifact` | Review result or recommendation artifact. |
| `validation_artifact` | Deterministic validation result. |
| `audit_artifact` | Audit or verification artifact. |

### Usage Rules

- Permanent Layer 2 platform constitution documents use
  `doc_type: "platform_standard"`.
- Layer 3 bundle outputs use `doc_type: "workflow_output"` or
  `doc_type: "bundle_definition"` depending on whether the document is
  a delivery output or a bundle control document.
- Temporary evidence artifacts use the appropriate artifact type
  (`review_artifact`, `validation_artifact`, `audit_artifact`).
- No document on this platform may claim `doc_type: "masterplan"`
  unless it is a human-authored masterplan in the `masterplan/`
  directory.

## Platform authority Values

The Layer 1 Metadata Standard defines the baseline `authority`
vocabulary. This platform clarifies how authority values apply within
the agent-runner-v2 context.

### Platform-Specific Authority Values

| Value | Meaning | Usage |
|---|---|---|
| `platform-owned` | Canonical content is owned by the agent-runner-v2 platform core. | Used for platform constitution documents that are maintained as living standards. |
| `bundle-owned` | Canonical content is owned by a specific Layer 3 bundle. | Used for bundle-local definitions that are not platform-wide. |

### Inherited Values (from Layer 1)

| Value | Meaning | Usage on This Platform |
|---|---|---|
| `human-authored` | Canonical content is maintained directly by humans. | Masterplan documents, manually maintained guides. |
| `workflow-generated` | Canonical content is produced and maintained by a workflow. | Platform constitution documents generated by the Layer 2 workflow. |
| `derived` | Content is generated from another authoritative source. | Adapted or transformed content, not itself root authority. |

### Usage Rules

- Permanent Layer 2 documents use `authority: "workflow-generated"`
  when produced by the platform constitution workflow.
- After publication and ongoing maintenance, documents may transition
  to `authority: "platform-owned"`.
- Layer 3 bundle outputs use `authority: "bundle-owned"` or
  `authority: "workflow-generated"` depending on ownership model.
- No generated document may claim `authority: "human-authored"`.

## Additional Frontmatter Fields

Beyond the Layer 1 baseline fields, this platform defines the following
additional frontmatter fields for its artifacts.

### Platform Field

| Field | Required | Description |
|---|---|---|
| `platform` | Yes (all Layer 2 and Layer 3 permanent docs) | Identifies the platform: `agent-runner-v2`. |

This field distinguishes platform-specific documents when multiple
Layer 2 cores share an ecosystem.

### Template ID Field

| Field | Required | Description |
|---|---|---|
| `template_id` | Yes (permanent docs) | Stable identifier for structural compliance checking. |

Template IDs follow the pattern `SYS-<layer>-<abbrev>` where:

- Layer 1 uses `SYS-00-*` (e.g., `SYS-00-IDX`, `SYS-00-LM`).
- Layer 2 uses `SYS-02-*` (e.g., `SYS-02-IDX`, `SYS-02-RM`).
- Layer 3 uses bundle-specific template IDs.

### Managed By Field

| Field | Required | Description |
|---|---|---|
| `managed_by` | Conditional | Indicates the document is workflow-managed. Required for workflow-generated documents. |

When present, this field signals that the document is protected from
manual edits and is maintained by a specific workflow.

### Effective Version Field

| Field | Required | Description |
|---|---|---|
| `effective_version` | Conditional | Run or change identifier. Required for workflow-generated permanent documents. |

The value is the job ID of the workflow run that produced or last
updated the document (e.g., `02PC-20260720-86359b88`).

## Inheritance Rules

Metadata conventions inherit downward across layers on this platform:

### Layer 1 to Layer 2

- Layer 2 inherits all Layer 1 baseline fields and vocabularies.
- Layer 2 may extend `doc_type` and `authority` value sets.
- Layer 2 must not redefine the meaning of Layer 1 baseline values.
- Layer 2 may add new fields (e.g., `platform`) that Layer 1 does not
  define.

### Layer 2 to Layer 3

- Layer 3 inherits Layer 1 baseline plus Layer 2 platform extensions.
- Layer 3 must include all required fields from both layers.
- Layer 3 must set `platform: "agent-runner-v2"` on all permanent
  documents.
- Layer 3 must set `layer: "layer3"` on all bundle outputs.
- Layer 3 may add bundle-specific fields for its own needs.
- Layer 3 must not redefine Layer 1 or Layer 2 field meanings.

### Inheritance Chain

```
Layer 1 (baseline fields + vocabularies)
    |
    v
Layer 2 (platform extensions: platform field, platform_standard doc_type, platform-owned authority)
    |
    v
Layer 3 (bundle-specific fields, inherits all above)
```

## Scan Policy Expectations

This platform defines scan-policy expectations for its artifacts.

### Permanent Platform Documents

All permanent Layer 2 platform constitution documents carry:

```yaml
scan_policy: "include"
scan_reason: "permanent Layer 2 platform constitution; must be included in operational scans"
```

### Temporary Evidence Artifacts

Temporary artifacts (review, validation, audit) carry:

```yaml
scan_policy: "conditional"
scan_reason: "temporary evidence artifact; include only when explicitly requested"
```

or:

```yaml
scan_policy: "exclude"
scan_reason: "temporary run-scoped artifact; exclude from operational scans"
```

### Layer 3 Bundle Documents

Layer 3 bundle documents follow these expectations:

| Document Class | Typical scan_policy |
|---|---|
| Bundle definitions (permanent) | `include` |
| Workflow outputs (permanent) | `include` |
| Review artifacts (temporary) | `conditional` or `exclude` |
| Validation artifacts (temporary) | `conditional` or `exclude` |
| Audit artifacts (temporary) | `conditional` or `exclude` |

### Scanner Compliance

All scanners operating on this platform's documents must honor the
Layer 1 scanner compliance rules:

1. Honor explicit `scan_policy` metadata.
2. Respect `exclude` documents unless explicitly targeted.
3. Require explicit inclusion rule for `conditional` documents.
4. Report non-compliance for malformed metadata.
5. Never assume authority from absence of metadata.
