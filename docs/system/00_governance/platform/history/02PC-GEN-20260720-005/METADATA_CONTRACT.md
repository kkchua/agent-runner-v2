---
template_id: SYS-02-MC
version: "1.0"
doc_type: "platform_standard"
authority: "platform-owned"
scan_policy: "include"
scan_reason: "permanent Layer 2 metadata contract; defines platform-specific metadata extensions"
layer: "layer2"
platform: "agent-runner-v2"
lifecycle_status: "published"
effective_version: "02PC-GEN-20260720-005"
managed_by: workflow-generated
---

> Managed by workflow: `02_platform_core_foundation_v1` / step: `publish_platform_core_set`
> This file is workflow-generated and protected from manual edits.

# Metadata Contract

## Purpose

This document defines the platform-specific metadata extensions for
agent-runner-v2 beyond the Layer 1 metadata baseline. It specifies
additional `doc_type` values, additional `authority` values, additional
frontmatter fields, inheritance rules, and scan policy expectations that
apply to documents produced on this platform.

This contract inherits from the Layer 1 Metadata Standard
(`docs/system/00_governance/foundation/current/METADATA_STANDARD.md`).
It does not redefine any Layer 1 baseline field or vocabulary. It extends
where the platform requires more specific classification.

## Platform doc_type Values

The Layer 1 Metadata Standard defines the baseline `doc_type` vocabulary.
This platform uses the following subset and adds no new `doc_type` values
beyond those defined by Layer 1.

### Values Used by This Platform

| Value | Meaning | Typical Layer |
|---|---|---|
| `platform_standard` | A Layer 2 operating standard for agent-runner-v2. | Layer 2 |
| `system` | System-level governance document part of a governed set. | Layer 1 or Layer 2 |
| `bundle_definition` | A bundle-local definition or control document owned by a workflow bundle. | Layer 3 |
| `workflow_output` | Concrete output generated by a workflow execution. | Layer 3 |
| `review_artifact` | Review result, critique, or recommendation artifact. | Layer 2 or Layer 3 |
| `validation_artifact` | Deterministic validation result or compliance artifact. | Layer 2 or Layer 3 |
| `audit_artifact` | Audit or verification artifact created after review or validation. | Layer 2 or Layer 3 |
| `masterplan` | Human-authored reference blueprint used to guide design decisions. | Layer 1 |

### Platform Standard Classification

Documents that define the agent-runner-v2 platform constitution must use
`doc_type: "platform_standard"`. This distinguishes them from:

- Layer 1 governance documents (`doc_type: "system"`).
- Layer 3 bundle outputs (`doc_type: "workflow_output"` or
  `doc_type: "bundle_definition"`).
- Temporary evidence artifacts (`doc_type: "review_artifact"`,
  `doc_type: "validation_artifact"`, or `doc_type: "audit_artifact"`).

### Bundle Definition Classification

Documents that define bundle-local governance, prompt contracts, or
action policies within a Layer 3 workflow bundle must use
`doc_type: "bundle_definition"`. These documents are owned by the bundle
and are not platform-wide standards.

## Platform authority Values

The Layer 1 Metadata Standard defines the baseline `authority` vocabulary.
This platform uses the following values.

### Values Used by This Platform

| Value | Meaning | Typical Usage |
|---|---|---|
| `workflow-generated` | Canonical content is produced and maintained by a workflow. | Platform constitution docs, temporary evidence |
| `platform-owned` | Canonical content is owned by the agent-runner-v2 platform core. | Active published platform standards |
| `bundle-owned` | Canonical content is owned by a specific Layer 3 bundle. | Bundle-local definitions |
| `human-authored` | Canonical content is maintained directly by humans. | Masterplan documents |
| `derived` | Content is generated from another authoritative source. | Context inventories, derived reports |

### Authority Assignment Rules

- Permanent Layer 2 platform constitution documents use
  `authority: "workflow-generated"` during staged runs and
  `authority: "platform-owned"` after publication.
- Layer 3 bundle outputs use `authority: "bundle-owned"` or
  `authority: "workflow-generated"` depending on whether the bundle
  or the generating workflow owns the canonical truth.
- Temporary evidence artifacts (review, validation, audit) use
  `authority: "workflow-generated"`.
- No generated document may claim `authority: "human-authored"`.
- No derived artifact may present itself as root authority.

## Additional Frontmatter Fields

Beyond the Layer 1 baseline fields, this platform requires the following
additional frontmatter fields for documents produced on agent-runner-v2.

### Required Platform Fields

| Field | Required | Description |
|---|---|---|
| `platform` | Yes (Layer 2 and Layer 3) | Identifies the owning platform. Must be `"agent-runner-v2"` for all documents in this platform's scope. |
| `template_id` | Yes (permanent docs) | Stable identifier for structural validation. Follows the pattern `SYS-<layer>-<abbrev>` (e.g., `SYS-02-RM`, `SYS-00-IDX`). |
| `managed_by` | Conditional | Required for workflow-generated documents. Indicates the document is workflow-managed and protected from manual edits. |

### Optional Platform Fields

| Field | Required | Description |
|---|---|---|
| `effective_version` | Conditional | Run or job identifier for version tracking. Required for workflow-generated permanent documents. |
| `generated_at` | No | ISO 8601 timestamp of when the document was generated. Used for temporary evidence artifacts. |

### Field Rules

- The `platform` field must be present on all Layer 2 and Layer 3
  documents. It must match the owning platform identifier.
- The `template_id` field must follow the platform's naming convention:
  `SYS-<layer-number>-<abbrev>` where `<layer-number>` is the two-digit
  layer number and `<abbrev>` is a short uppercase identifier.
- The `managed_by` field must be present when `authority` is
  `"workflow-generated"`.
- The `effective_version` field must contain the job ID or run identifier
  that produced the document.

## Inheritance Rules

Metadata conventions inherit downward across layers on this platform:

### Layer 1 Baseline

Layer 1 defines:

- Required field names: `doc_type`, `authority`, `scan_policy`,
  `scan_reason`.
- Extended field names: `template_id`, `version`, `layer`,
  `lifecycle_status`, `effective_version`, `managed_by`.
- Baseline vocabularies for all fields.
- Scan policy rules and scanner compliance expectations.

### Layer 2 Extensions

Layer 2 (this platform) adds:

- The `platform` field requirement for Layer 2 and Layer 3 documents.
- Platform-specific `template_id` naming convention.
- Clarification of `authority` transitions during the publish lifecycle
  (`workflow-generated` to `platform-owned` for permanent docs).

### Layer 3 Obligations

Layer 3 bundles must:

- Include all Layer 1 baseline fields.
- Include all Layer 2 platform extension fields.
- Use `layer: "layer3"` and `platform: "agent-runner-v2"`.
- Not redefine any Layer 1 or Layer 2 field meaning.
- Not use `doc_type: "platform_standard"` for bundle outputs.
- Not use `authority: "platform-owned"` for bundle outputs.

### Inheritance Direction

```
Layer 1 (baseline fields + vocabularies)
    |
    v
Layer 2 (platform extensions + platform field)
    |
    v
Layer 3 (must include L1 + L2 fields, apply platform values)
```

No lower layer may redefine the meaning of a higher layer's field or
vocabulary value.

## Scan Policy Expectations

This platform applies the Layer 1 scan policy rules with the following
platform-specific expectations.

### Permanent Platform Standards

- `scan_policy: "include"` for all permanent Layer 2 platform
  constitution documents.
- These documents participate in operational scans and compliance checks.

### Temporary Evidence Artifacts

- `scan_policy: "exclude"` for run-scoped evidence artifacts (context
  inventories, review outputs, validation outputs, audit outputs).
- These documents are excluded from operational scans unless explicitly
  targeted by a review or audit workflow.

### Layer 3 Bundle Outputs

- `scan_policy: "include"` for permanent bundle outputs that
  participate in governed document sets.
- `scan_policy: "conditional"` for bundle-internal definitions that
  are only relevant during bundle-specific scans.
- `scan_policy: "exclude"` for bundle-local temporary evidence.

### Scan Reason Requirements

- Every document with `scan_policy: "exclude"` must provide a non-empty
  `scan_reason` explaining why it is excluded.
- Every document with `scan_policy: "conditional"` must provide a
  non-empty `scan_reason` explaining the inclusion conditions.

### Scanner Compliance

Scanners operating on this platform must follow the Layer 1 scanner
compliance rules:

1. Honor explicit `scan_policy` metadata when present.
2. Skip `exclude` documents unless explicitly targeted.
3. Require explicit inclusion rules for `conditional` documents.
4. Report non-compliant metadata (missing fields, invalid values).
5. Not assume authority from document presence alone.
