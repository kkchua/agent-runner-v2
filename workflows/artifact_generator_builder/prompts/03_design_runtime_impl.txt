# Objective

Design the runtime implementation that executes the composition spec to produce output from input.

# MANDATORY: BASE_COMPOSITION_STANDARD_v1.0.md Section 13.8

You MUST follow BASE_COMPOSITION_STANDARD_v1.0.md Section 13.8 ({BASE_COMPOSITION_STANDARD}). This section defines the override pattern:
- The `workflow.toml` IS the default implementation — every step has its prompt or action assigned
- Alternative implementations use `impls/{name}/impl.yaml` for partial overrides only
- No separate "default impl mapping" file is needed

# Reference Inputs

- COMPOSITION_SPEC_FILE: {COMPOSITION_SPEC_FILE}
- REQUIREMENT_ANALYSIS_FILE: {REQUIREMENT_ANALYSIS_FILE}
- REQUIREMENT_DOC: {REQUIREMENT_DOC}
- BASE_COMPOSITION_STANDARD: {BASE_COMPOSITION_STANDARD}
- CODENAME: {CODENAME}

# Generation Tasks

Produce a runtime implementation design containing:

1. **Implementation Architecture** -- High-level structure, component modules, data flow
2. **Input Loading** -- How to load, parse, and validate input
3. **Transformation Engine** -- Step-by-step processing, how composition spec rules are applied, error handling
4. **Output Generation** -- How to produce ALL output artifacts defined in the requirement doc
5. **Configuration** -- Runtime parameters, defaults, overrides
6. **Extension Interface** -- How to add new implementations via `impls/{name}/impl.yaml`
7. **Default Implementation** -- For each step in the workflow, specify which prompt template or action function handles it. This mapping goes directly into `workflow.toml` (not a separate file).

# Output Instructions

Write ONE file:

1. {RUNTIME_IMPL_FILE} -- Runtime implementation design (doc_type: "runtime_impl")

This document must include complete algorithm descriptions, data structures, configuration defaults, and extension point implementations. It must clearly specify the default prompt/action assignment for every step — these assignments will be written directly into `workflow.toml` during package generation.

identity_locked: true, generator_name: "{CODENAME}", codename: "{CODENAME}". ASCII-only.

# Forbidden Content

- Do NOT reference the builder's identity
- Do NOT deviate from the composition spec
- Do NOT include non-ASCII characters
- Do NOT produce a separate "default impl" file — workflow.toml IS the default implementation
