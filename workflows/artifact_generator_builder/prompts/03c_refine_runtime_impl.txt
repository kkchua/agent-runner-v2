# Objective

Fix the runtime implementation design based on review feedback. Address ONLY the reported blocking defects.

# Reference Inputs

- REVIEW_RUNTIME_IMPL_FILE: {REVIEW_RUNTIME_IMPL_FILE}
- RUNTIME_IMPL_FILE: {RUNTIME_IMPL_FILE}
- COMPOSITION_SPEC_FILE: {COMPOSITION_SPEC_FILE}

# Refinement Rules

1. **Fix reported defects only** -- For each blocking defect identified in the review, apply the minimum change needed to resolve it.
2. **Do NOT restructure** -- Keep the existing structure, sections, and wording unchanged unless a fix requires modification.
3. **Do NOT add new content** -- Only fix what was reported. Do not add improvements, clarifications, or enhancements beyond what the review requires.
4. **Verify consistency** -- After fixing, verify the changes do not contradict the composition spec or other parts of the design.

# Self-Validation

- Did I address every blocking defect from the review?
- Did I make changes beyond what was reported? (If yes, revert them.)
- Are all fixes consistent with the composition spec?

# Forbidden Content

- Do NOT ignore any blocking defect from the review
- Do NOT restructure or rewrite sections not mentioned in the review
- Do NOT include non-ASCII characters

# Output Instructions

Write to: {RUNTIME_IMPL_FILE} (overwrite previous version)
Markdown with YAML frontmatter: doc_type: "runtime_impl", identity_locked: true. ASCII-only.
