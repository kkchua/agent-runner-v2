# Objective

Write the **domain logic** for the workflow package: the action functions in
actions.py and the prompt templates in prompts/. You are following the Domain
Plan as your blueprint — you are NOT designing, you are implementing.

# MANDATORY: Follow the Domain Plan

Read {AGB_DOMAIN_PLAN} completely before writing any code. For each planned
action and prompt, implement exactly what the plan specifies. The plan's
acceptance criteria are your completion criteria.

If {AGB_IMPL_CRITIQUE} exists, you are in a refinement loop. Read the critique
and address every finding before proceeding.

# MANDATORY: Read Domain Artifact Map for Available Artifacts

Before writing ANY code or prompts, you MUST READ {AGB_DOMAIN_MAP}.
This file defines ALL available artifact keys for this workflow's domain logic.

**Rules:**
1. Your actions and prompts CAN ONLY reference artifact keys that exist in
   the Domain Artifact Map. Do NOT invent new artifact keys.
2. For each step in the Domain Plan, check the data flow to see which
   artifacts that step consumes (inputs) and produces (outputs). Only reference
   those specific artifacts in the step's actions and prompts.
3. The Domain Artifact Map is derived from the Analysis JSON — it is the
   single source of truth for domain artifact bindings.

**Why this matters:** The Domain Artifact Map shows exactly which keys exist
and how they flow between steps. By reading it first, you eliminate all
artifact binding guesswork.

# Reference Inputs

- AGB_DOMAIN_PLAN: {AGB_DOMAIN_PLAN}
  The blueprint. Tells you WHAT to implement and the acceptance criteria.

- AGB_ANALYSIS_JSON: {AGB_ANALYSIS_JSON}
  The Analysis JSON. Defines identity, step names, and step types.

- AGB_DOMAIN_MAP: {AGB_DOMAIN_MAP}
  The domain artifact key mapping. Defines ALL available artifact keys and
  their data flow. You MUST READ this file before writing any code or prompts.

- BASE_COMPOSITION_STANDARD: {BASE_COMPOSITION_STANDARD}
  Action interface contract (Section 5). Your functions MUST conform.

- AGB_IMPL_CRITIQUE: {AGB_IMPL_CRITIQUE}
  If this file exists, it contains findings from the critic (step 6). You MUST
  address every BLOCKING and MAJOR finding.

# Implementation Protocol

## Part A: Write actions.py

Write to: {DOMAIN_ACTIONS_FILE}

### Required Structure

```python
"""Domain actions for {workflow_name}.

Auto-generated by AGB v3. Implements domain-specific action functions.
"""
from __future__ import annotations

import json
from pathlib import Path
from typing import Any

from agent_runner_v2.action_result import ActionResult
from agent_runner_v2.workflow_packages.actions import action


@action("action_name_here")
def action_name_here(*, context, state, step_cfg, project_root):
    """One-line description of what this action does."""
    project_root = Path(project_root)

    # Read input paths from context (resolved by context_extensions.py)
    input_path = context.get("INPUT_KEY")

    # Resolve output path from context (NOT state["artifacts"])
    output_path_str = context.get("OUTPUT_KEY")
    if not output_path_str:
        return ActionResult(
            status="REJECTED",
            remark="OUTPUT_KEY output path not found in context",
            artifacts={},
            reject_code="MISSING_OUTPUT_PATH",
        )
    output_path = Path(output_path_str)
    output_path.parent.mkdir(parents=True, exist_ok=True)

    # ... implementation ...

    return ActionResult(
        status="APPROVED",
        remark="Description of what was done",
        artifacts={"OUTPUT_KEY": str(output_path)},
    )
```

### MANDATORY Action Rules

1. **Decorator**: Every action function MUST be decorated with
   `@action("action_name")` where action_name matches the Analysis JSON's
   domain_steps[].action_name.

2. **Signature**: Every action function MUST use keyword-only arguments:
   `(*, context, state, step_cfg, project_root)`.

3. **Return type**: Every action function MUST return an `ActionResult` with
   status ("APPROVED" or "REJECTED"), remark (string), and artifacts (dict).

4. **Boundary validation**: Every action MUST validate its inputs at the
   function boundary. Check that required artifact paths exist, are readable,
   and contain parseable data. Return REJECTED with a descriptive reject_code
   on validation failure.

5. **Complete implementations**: Every function MUST contain a COMPLETE,
   WORKING implementation. No stubs, no placeholder fallbacks, no
   "TODO: implement" comments. If a function claims to support a format,
   it MUST contain real extraction logic for that format.

6. **No false format claims**: If the function cannot genuinely parse a format
   (e.g., binary PDF with no text layer), do NOT claim to support it. If a
   format requires an external library, implement real parsing logic using
   that library and declare the dependency in requirements.txt. Do NOT
   silently drop formats that the requirement doc asks for.

7. **Meaningful error paths**: Every branch MUST produce a meaningful result.
   No dead-code branches where all paths return the same value. Every REJECTED
   path MUST have a distinct reject_code.

8. **Artifact writes**: When writing output artifacts, the function MUST read
   the output path from `context` (not `state["artifacts"]`). The context dict
   contains resolved absolute paths for all artifact keys. Use
   `context.get("OUTPUT_KEY")` to get the output path. Create parent
   directories as needed.

## Part B: Write Prompt Files

Write each prompt template to: {DOMAIN_PROMPTS_DIR}{prompt_filename}

### MANDATORY Prompt Rules

1. **Input section**: Every prompt MUST clearly list what inputs the LLM
   receives, using curly-brace syntax for artifact paths (e.g., `{ARTIFACT_KEY_NAME}`).
   Example: `PARSED_DOCUMENT: {PARSED_DOCUMENT}`

2. **Output section**: Every prompt MUST specify exactly where to write the
   output and what format to use.
   Example: `Write to: {TRANSFORMED_CONTENT}`

3. **Domain instructions**: The prompt MUST contain all domain-specific
   instructions from the Domain Plan. Do not assume the LLM knows the domain.

4. **Validation checklist**: Every prompt MUST include a self-validation
   section before the output instructions. The checklist items MUST be
   specific to the domain (not generic "check your work").

5. **Placeholder consistency**: Every artifact placeholder (text in curly braces
   like `{ARTIFACT_KEY}`) referenced in the prompt MUST correspond to an actual
   artifact key in the Domain Artifact Map. Do not use generic example
   text like `{PLACEHOLDER}` — only reference real artifact keys that exist.

   **Documentation examples**: When showing placeholder structure in documentation
   or code examples within the prompt, use angle bracket syntax (`<ARTIFACT_KEY>`)
   instead of curly braces to avoid confusion with runtime placeholders.

6. **Forbidden content in prompts**:
   - Do NOT include non-ASCII characters

## Part C: Write impl.yaml

Write to: `{DOMAIN_ACTIONS_FILE}` directory (i.e., same folder as actions.py)
Filename: `impl.yaml`

The impl.yaml declares the implementation metadata and any step-specific
overrides (e.g., different action names or prompt slots).

Example structure:
```yaml
name: standard
description: "Default implementation for {workflow_name}"
label: "Standard"

# Optional: step-specific overrides
overrides:
  step_name:
    action: custom_action_name
    prompt: custom/prompt/path.txt
```

For a standard implementation without overrides, write a minimal file:
```yaml
name: standard
description: "Default implementation for {workflow_name}"
label: "Standard"
```

## Part D: Write requirements.txt

Write to: `{DOMAIN_ACTIONS_FILE}` directory (i.e., same folder as actions.py)
Filename: `requirements.txt`

You MUST always create a requirements.txt file in the same directory as
actions.py, even if the workflow has no external dependencies.

If external libraries are used in actions.py, list ALL of them — one package
per line, with version pins where appropriate.

Example (with dependencies):
```
PyPDF2>=3.0.0
python-docx>=1.0.0
openpyxl>=3.1.0
```

If no external dependencies are needed, write a file with a single comment:
```
# No external dependencies required — all imports are from the Python standard library.
```

Rules:
- Only include packages that are actually imported in actions.py
- Do NOT include Python standard library modules (json, pathlib, shutil, etc.)
- Use `>=` version pins for minimum versions when known

# meta.json Reporting

When writing your meta.json sidecar, report ONLY these artifact keys:

- `DOMAIN_ACTIONS_FILE` — path to the shared actions.py
- `DOMAIN_PROMPTS_DIR` — path to the prompts/ directory
- `DOMAIN_IMPL_FILE` — path to the impl.yaml (optional, for future multi-impl support)

Reporting undeclared artifact keys causes a STEP_CONTRACT_MISMATCH rejection.

# Self-Validation

Before reporting completion, verify:

- [ ] Every action function in the Domain Plan has a corresponding @action function in actions.py
- [ ] Every prompt file in the Domain Plan has been written to the prompts/ directory
- [ ] impl.yaml exists in the same directory as actions.py with valid YAML structure
- [ ] All action functions use the correct signature: (*, context, state, step_cfg, project_root)
- [ ] All action functions return ActionResult with status, remark, and artifacts
- [ ] All action functions validate inputs at boundaries and return REJECTED on failure
- [ ] No stub implementations — every function has complete working logic
- [ ] All prompt files reference only artifact keys that exist in the Domain Artifact Map
- [ ] All prompt artifact references match the Domain Plan's data flow for that step
- [ ] Documentation examples in prompts use angle bracket syntax (<ARTIFACT_KEY>) not curly braces
- [ ] All prompt files have input sections, output instructions, and validation checklists
- [ ] Action names in @action decorators match the Analysis JSON's domain_steps[].action_name
- [ ] If AGB_IMPL_CRITIQUE existed, every BLOCKING/MAJOR finding has been addressed

# Forbidden Content

- Do NOT write any files outside of {DOMAIN_ACTIONS_FILE} and {DOMAIN_PROMPTS_DIR} directories.
- Do NOT include non-ASCII characters.
- Do NOT skip any action or prompt from the Domain Plan.
