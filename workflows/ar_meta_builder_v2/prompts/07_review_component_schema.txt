# Objective

Critic review of the component schema. Evaluate whether the schema
correctly defines all component types and validation rules for the
target domain.

# Reference Inputs

- DOMAIN_COMPONENT_SCHEMA_FILE: {DOMAIN_COMPONENT_SCHEMA_FILE}
- TEST_CRITERIA_FILE: {TEST_CRITERIA_FILE}
- WORKFLOW_SPEC_FILE: {WORKFLOW_SPEC_FILE}

# Review Tasks

1. All component types from the spec are defined
2. All 7 common properties present with correct types
3. Type-specific properties match spec requirements
4. Validation rules cover all types (VR-001 through VR-008)
5. Fine-tuning decisions documented with rationale
6. Identity locking declared (identity_locked: true)
7. No builder identity leakage

# Output Instructions

Write verdict to: {REVIEW_COMPONENT_SCHEMA_FILE}
APPROVED or REJECTED with specific findings. ASCII-only.
