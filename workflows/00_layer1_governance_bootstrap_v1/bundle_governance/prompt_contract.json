{
  "version": 1,
  "defaults": {
    "ascii_only": true
  },
  "step_requirements": {
    "generate_layer1_governance_docs": {
      "required_ordered_literals": [
        "Read first:",
        "Hard ownership rules:",
        "Layering model to preserve:",
        "Layer 1 must define:",
        "Layer 1 must not define:",
        "Required frontmatter:",
        "Required sections:",
        "Document-specific rules:",
        "Before returning final JSON:"
      ],
      "required_literals": [
        "Use ASCII characters only",
        "single-workflow",
        "multi-workflow",
        "global runtime home as the canonical runtime source",
        "must not define repo-local workflow fallback"
      ]
    },
    "review_layer1_governance_docs": {
      "required_ordered_literals": [
        "Preflight Gate (REVIEW MODE)",
        "Role:",
        "Inputs:",
        "Freshness requirement:",
        "Review criteria:",
        "Reject if any file:",
        "Approve only if all of these are true:",
        "Task:",
        "Required JSON schema:"
      ],
      "required_literals": [
        "no reviewed file contains mojibake or non-ASCII punctuation corruption",
        "dual-path discovery or local fallback",
        "runtime path resolution policy"
      ]
    },
    "refine_layer1_governance_docs": {
      "required_ordered_literals": [
        "Read first:",
        "Task:",
        "Preserve these rules while refining:",
        "Enforce these scope rules while refining:",
        "Before returning final JSON, re-read the four files and confirm:"
      ],
      "required_literals": [
        "all four files must remain ASCII-only and free of mojibake",
        "delete any dual-path discovery, global-first/local-fallback, or repo-local",
        "replace any smart punctuation or mojibake with plain ASCII punctuation"
      ]
    },
    "audit_layer1_governance_accuracy": {
      "required_ordered_literals": [
        "Preflight Gate (SEMANTIC AUDIT MODE)",
        "Role:",
        "Inputs:",
        "Audit requirements:",
        "Task:",
        "Required JSON schema:"
      ],
      "required_literals": [
        "repo-local fallback or dual-path discovery",
        "runtime path resolution policy",
        "generated docs are ASCII-only"
      ]
    }
  }
}
