# agent-runner-v2 runtime dependencies
# Install with: pip install -r requirements.txt

# Core runtime dependencies
requests>=2.31.0
python-dotenv>=1.0.0
tomli>=2.0.0; python_version < "3.11"

# Optional dependencies
# psutil>=5.9.0  # Detailed worker process info in status command (graceful degradation without it)

# Development dependencies (install with: pip install -e ".[dev]")
# pytest>=8.2.0
# pytest-cov>=5.0.0
