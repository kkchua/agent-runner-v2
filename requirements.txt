# agent-runner-v2 runtime dependencies
# Install with: pip install -r requirements.txt

# Core dependencies (should already be installed via UKBE)
# requests>=2.31.0
# jinja2>=3.1.0

# Notification support
python-dotenv>=1.0.0
flet

# Development dependencies (optional)
# pytest>=8.2.0
# pytest-cov>=5.0.0
