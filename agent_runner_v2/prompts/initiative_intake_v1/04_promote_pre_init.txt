Runner Gate (MANDATORY)
- The human has approved the pre-init artifact.
- Treat invocation as authorization to promote the pre-init to an official INIT artifact.

GOVERNING RULES (STRICT):
- Read {PRE_INIT_FILE} from disk
- Copy it to {INIT_FILE_PATH} (exact path from runner)
- Update metadata in the copy:
  - `Status: Approved` (was `Draft`)
  - `Approved At: <current ISO timestamp>`
  - Remove the `## 📝 Notes` traceability section — replace with a single line:
    `Promoted from: <pre_init file path>`
- Do NOT modify the original {PRE_INIT_FILE}
- Do NOT create plans, tasks, or any downstream artifacts

Completion rule (MANDATORY)
- You may return `status: "APPROVED"` only after:
  1. The INIT file has been written to {INIT_FILE_PATH}
  2. The file exists on disk
  3. The Status is `Approved`
  4. The meta.json sidecar has been written to {INIT_FILE_METAJSON}
- If any checks fail, return `REJECTED`

Critical: meta.json sidecar is REQUIRED
- Sidecar path: {INIT_FILE_METAJSON}
- Sidecar structure (exact):
  {
    "schema_version": "v2",
    "coder_result": {
      "status": "APPROVED" | "REJECTED",
      "remark": "Brief summary",
      "artifacts": {
        "INIT_FILE": "<exact path>"
      },
      "recorded_at": "<ISO-8601 timestamp>"
    }
  }
- Use `write_file` tool to create the sidecar immediately after writing the INIT file.

Output file path (MANDATORY)
- Write the INIT markdown file to: {INIT_FILE_PATH}
- Write the meta.json sidecar to: {INIT_FILE_METAJSON}

Return ONLY valid JSON:
{
  "schema_version": "v2",
  "coder_result": {
    "status": "APPROVED | REJECTED",
    "remark": "<short summary>",
    "artifacts": {
      "INIT_FILE": null
    },
    "recorded_at": "<ISO-8601 timestamp>"
  }
}
