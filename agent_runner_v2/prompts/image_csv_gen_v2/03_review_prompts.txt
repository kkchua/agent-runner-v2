TASK
Review the generated WAN 2.2 T2V prompt output in {IMAGE_CSV_JSON}.
Read the JSON and CSV files in the run directory, validate each variation against the quality rules, and produce a review decision.

REVIEW CRITERIA
For EACH image's 4 variations, verify:

1. STRUCTURAL VALIDATION
   - Exactly 4 items per image
   - All image_filename values are unique and follow format: image_NN_theme_style.png
   - Each item has exactly 5 keys: image_filename, t2i_prompt1, t2i_prompt2, negative_prompt, workflowKey
   - workflowKey is present and non-empty
   - No extra keys, no missing keys

2. VARIATION ENGINE VALIDATION
   - All 4 rows use unique camera direction phrases
   - No identical time + weather + lighting combination across 4 rows
   - Particle effect types differ between rows
   - Color temperature descriptions vary

3. t2i_prompt1 VALIDATION (WAN 2.2 T2V BASE PROMPT — CHINESE)
   - Length: 50–300 Chinese characters (字)
   - Begins verbatim with an approved camera direction phrase:
     镜头缓慢向前推进 | 镜头缓慢向后拉远 | 镜头缓慢向左横移 |
     镜头缓慢向右横移 | 镜头缓慢向上仰拍 | 镜头缓慢向下俯拍 |
     缓慢环绕 | 螺旋上升 | 弧形移动 | 跟随追踪 | 定点旋转 | 波浪式移动 |
     微距推进 | 轻微摇摆 | 呼吸式移动 | 渐变聚焦 | 平移扫描 | 滑动跟随
   - Contains all 6 structural blocks: camera opening, subject identity, environment/spatial depth, lighting/atmosphere, motion/animation, mood/cinematic tone
   - Contains 前景/中景/远景 three-layer depth descriptions
   - Contains 2–3 subtle natural motion descriptions
   - Contains anti-collage sentence: 画面为单一连续场景，无分割线、无边框、无拼贴布局。
   - Ends with hard closing anchor: 从第一帧到最后一帧，绝无反向、无回退、无方向改变。
   - NO static framing words (居中、摆姿势、静止、定格)
   - NO cut/transition terms (跳切、切换、然后、接着)
   - NO bidirectional language (回、返、再、反转、循环、来回)
   - Pure Chinese natural language only

4. t2i_prompt2 VALIDATION (WAN 2.2 T2V CONTINUATION PROMPT — CHINESE)
   - Length: 50–300 Chinese characters (字)
   - Begins with continuation wording (镜头继续缓慢地…… | 延续之前的节奏…… | 在相同的场景中，镜头持续……)
   - Camera direction is identical to t2i_prompt1 (rephrased, not repeated verbatim)
   - Contains all 5 structural blocks: continuation camera statement, evolved subject state, evolved environment state, intensified atmosphere, motion continuation
   - Maintains spatial continuity with t2i_prompt1 (same subject, environment, time of day)
   - Does NOT introduce new subjects, locations, or camera axes
   - Contains anti-collage sentence: 画面为单一连续场景，无分割线、无边框、无拼贴布局。
   - Ends with hard closing anchor: 从第一帧到最后一帧，绝无反向、无回退、无方向改变。

5. negative_prompt VALIDATION (CHINESE)
   - 15–55 Chinese characters, comma-separated
   - Covers quality, motion, exposure, and artifact issues
   - Does NOT contain positive prompt content

6. DIRECTIONAL SEMANTIC LOCK
   - All spatial and motion language in both prompts is consistent with the chosen camera direction
   - No conflicting motion vectors
   - Parallax enforcement: 前景/中景/远景 layers described as moving at distinct speeds

DECISION FORMAT
Write the review to: {REVIEW_FILE_PATH}

The review file must be a markdown document containing:
- A summary table of all images and their validation status
- Per-item details listing each pass/fail criterion with specific remarks
- A final decision: APPROVED or REJECTED
- If REJECTED: list specific issues that need to be fixed, referencing the exact image_filename and field (t2i_prompt1, t2i_prompt2, negative_prompt, or workflowKey)

review_prompts completion rule (MANDATORY)
- You may return `status: "APPROVED"` only after all of the following are true:
  - the review file has been written to {REVIEW_FILE_PATH}
  - the meta.json sidecar has been written to {REVIEW_FILE_METAJSON}
- If the review was not written successfully, you MUST return `REJECTED`.

Critical: meta.json sidecar is REQUIRED
- The runner reads the result from the sidecar file, NOT from your JSON output.
- Sidecar path: {REVIEW_FILE_METAJSON}
- Sidecar structure (exact):
  {
    "schema_version": "v2",
    "coder_result": {
      "status": "APPROVED" | "REJECTED",
      "remark": "Brief summary",
      "artifacts": {
        "REVIEW_FILE": "{REVIEW_FILE_PATH}"
      },
      "recorded_at": "<current ISO timestamp>",
      "reject_code": null | "PROMPT_VALIDATION_FAILED"
    }
  }
- Set reject_code to null if APPROVED, or "PROMPT_VALIDATION_FAILED" if REJECTED.
- Use write_file tool to create the sidecar immediately after writing the review file.
- You MUST verify the sidecar exists on disk before returning.

ABSOLUTE OUTPUT RULE (NON-NEGOTIABLE)

You are NOT a chat assistant.
You are a strict JSON generator.

Return ONLY valid JSON.
Do NOT explain.
Do NOT wrap in markdown.
Do NOT add any text before or after the JSON.
Do NOT wrap the final JSON in ```json fences.

Required JSON schema:
{
  "status": "APPROVED | REJECTED",
  "remark": "<short summary of result>",
  "artifacts": {
    "IMAGE_DESC_FOLDER": null,
    "IMAGE_CSV_JSON": null,
    "IMAGE_CSV_CSV": null,
    "REVIEW_FILE": null,
    "VALIDATION_FILE": null
  }
}

Rules for the artifacts object:
- Include ONLY artifact keys produced or updated by this step.
- Use repository-relative paths.
- Do not invent paths that were not created or updated.
- For approval, `artifacts.REVIEW_FILE` is mandatory.

Required execution sequence:
1. Read the JSON and CSV files from {IMAGE_CSV_JSON}.
2. Validate each variation against all review criteria above.
3. Write the review markdown file to {REVIEW_FILE_PATH}.
4. Write the meta.json sidecar to {REVIEW_FILE_METAJSON}.
5. Verify the sidecar exists on disk.
6. Return the final JSON with status APPROVED or REJECTED and artifacts.REVIEW_FILE set.

VALIDATION NOTES FOR REJECTION
If any of the following are found, set status to REJECTED:
- t2i_prompt1 length outside 50–300 Chinese characters
- t2i_prompt2 length outside 50–300 Chinese characters
- t2i_prompt1 does NOT begin with an approved camera direction phrase
- t2i_prompt2 does NOT begin with continuation wording
- Missing anti-collage sentence in either prompt
- Missing hard closing anchor in either prompt
- Bidirectional/reverse/loop/collage terminology present
- Parallax cues (前景/中景/远景) missing
- Extra or missing keys in JSON objects
