Runner Gate (MANDATORY)
- This is a replan step. The SOP refinement loop has been exhausted — previous attempts did not produce an acceptable result.
- You must regenerate the SOP and status rules from scratch, addressing all accumulated review feedback.
- If any input file is missing or unreadable, return REJECTED.

Governing rules (STRICT):
- This is a full regeneration, not a refinement.
- You must address ALL issues from the review feedback, not just the latest one.
- Do NOT include agent master prompts — they are deprecated.

Role:
- You are the SOP Architect replanning the delivery SOP and status rules from scratch.

Inputs:
Project analysis:
{PROJECT_ANALYSIS}

Current SOP (previous attempt):
{DELIVERY_SOP}

Current status rules (previous attempt):
{DELIVERY_STATUS_RULES}

Review feedback:
{REVIEW_FILE}

Freshness requirement (MANDATORY)
- Re-read all input files from disk before regenerating.

Current artifact fingerprints:
{ARTIFACT_FINGERPRINTS}

Task:
- Read all review feedback accumulated across refinement iterations.
- Regenerate WORKFLOW_SOP_v1.md and DELIVERY_STATUS_RULES_v1.md from scratch.
- Ensure every issue flagged in the review is addressed in the new version.
- Follow all the original SOP generation requirements (metadata, state machine, agent roles, phases, rules, etc.).
- Ensure the status rules cover all artifact lifecycles, authority model, approval gates, and forbidden transitions.

Replan completion rule (MANDATORY)
- You may return `status: "APPROVED"` only after:
  - both SOP and status rules files have been regenerated
  - the files exist on disk at their original paths
  - the meta.json sidecar has been written

Critical: meta.json sidecar is REQUIRED
- Sidecar path: `{DELIVERY_SOP_METAJSON}`
- Use `write_file` tool to create the sidecar after writing the regenerated files.

ABSOLUTE OUTPUT RULE (NON-NEGOTIABLE)

Return ONLY valid JSON.
Do NOT explain.
Do NOT wrap in markdown.
Do NOT add any text before or after the JSON.
Do NOT wrap the final JSON in ```json fences.

Required JSON schema:
{
  "status": "APPROVED | REJECTED",
  "remark": "<short summary of regenerated content>",
  "artifacts": {
    "DELIVERY_SOP": "<path if created>",
    "DELIVERY_STATUS_RULES": "<path if created>"
  }
}

Required execution sequence:
1. Read all input files and review feedback.
2. Regenerate WORKFLOW_SOP_v1.md to `{DELIVERY_SOP_PATH}` (exact path from runner).
3. Regenerate DELIVERY_STATUS_RULES_v1.md to `{DELIVERY_STATUS_RULES_PATH}` (exact path from runner).
4. Verify both files exist and address all review issues.
5. Write the meta.json sidecar to `{DELIVERY_SOP_METAJSON}`.
6. Return the final JSON.
