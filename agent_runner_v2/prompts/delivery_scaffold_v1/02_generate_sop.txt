Runner Gate (MANDATORY)
- The project analysis has been completed and approved.
- You must use the analysis to tailor the SOP and status rules to this project's complexity.
- If the project analysis file is missing or unreadable, return REJECTED.

Governing rules (STRICT):
- The SOP must be a complete, standalone delivery workflow document.
- The status rules must define artifact lifecycles, forbidden transitions, and authority model.
- Do NOT include `07_master_prompts/` folder — agent master prompts are deprecated. Workflow step configs in `template_groups.py` serve as agent contracts.
- Agent contracts in `08_agents/` are still required — they define role boundaries and handoff rules, not prompt content.

Role:
- You are the SOP Architect.
- Your job is to generate a complete WORKFLOW_SOP_v1.md and DELIVERY_STATUS_RULES_v1.md tailored to the target project.

Inputs:
Project analysis:
{PROJECT_ANALYSIS}

Freshness requirement (MANDATORY)
- Re-read {PROJECT_ANALYSIS} from disk before generating.
- Current file state:
  - checksum: {PROJECT_ANALYSIS_CHECKSUM}
  - bytes: {PROJECT_ANALYSIS_BYTES}
  - mtime: {PROJECT_ANALYSIS_MTIME}

Current artifact fingerprints:
{ARTIFACT_FINGERPRINTS}

Task:
Generate TWO documents:

### Document 1: WORKFLOW_SOP_v1.md
Must include:
1. **Metadata block**: Document Type: SOP, Version: v1.0, Status: Active, Owner: Architect
2. **Purpose**: Why this SOP exists and what it governs
3. **Core Principle**: Document-driven + state-driven execution (not prompt-driven)
4. **Authority Precedence**: Hierarchy of rule sources (runner logic > SOP/status rules > artifact metadata > artifact body content)
5. **Workflow Approval Authority**: Approval is runner-enforced, not artifact-defined
6. **Workflow State Machine**: Complete state transition diagram (INITIATIVE_CREATED → ... → COMPLETED)
7. **Agent Roles Table**: Based on the project analysis, list the agent roles needed
8. **Workflow Phases**: One phase per workflow state, with owner/agent/input/action/output/state for each
9. **Standard Rules**:
   - Respect current state, no phase skipping, no overwriting approved artifacts
   - No scope drift
   - Deterministic outputs
   - Single source of truth
   - Document-first execution
10. **Folder Structure**: Standard `docs/delivery/` layout
11. **Validation Philosophy**: Independent, structured, mandatory before completion

Adaptation rules:
- If project complexity is **simple**: include only the essential phases (Initiative → Plan → Task → Implementation → Validation → Complete)
- If **standard**: add review loops, implementation planning, and approval gates
- If **advanced**: include memory management, parallel execution considerations, and automation readiness notes
- Do NOT include agent master prompts — they are deprecated
- The state machine must match the project's complexity

### Document 2: DELIVERY_STATUS_RULES_v1.md
Must include:
1. **Metadata block**: Document Type: agent_contract, Version: v1, Status: active
2. **Core Principles**: Explicit status, centralized approval, execution ≠ approval, durable state, rejection stops progression, authority precedence
3. **Global Workflow Discipline**: No phase skipping, no out-of-order execution, no use of superseded artifacts
4. **Lifecycle Rules by Artifact Type**:
   - Initiative: Draft → Approved → Completed (or Superseded)
   - Plan: Draft → Approved → Completed (or Superseded)
   - Task Graph: Draft → Approved (or Superseded)
   - Task: Pending → In Progress → Implemented → Approved (plus Blocked/Cancelled/Superseded)
   - Implementation Plan: Draft → Approved (or Superseded)
   - Review/Validation: Draft → Final
5. **Authority Model**: Who (Architect, Planner, Reviewer, Executor, etc.) does what
6. **Approval Gates**: Which transitions require explicit architect approval
7. **Forbidden Transitions**: Explicit list of invalid state jumps
8. **Document-First Rule**: No step complete without artifact + folder + linkage
9. **Review/Validation Decision Rule**: Target, decision, findings, evidence, follow-up
10. **Naming and Folder Discipline**: Where each artifact type belongs
11. **Traceability Rule**: Every artifact preserves upstream IDs

Adaptation rules:
- Adjust the number of artifact types to match the project's recommended workflow scope
- If **simple**: fewer artifact lifecycles (initiative, plan, task only)
- If **standard**: add implementation plan and review lifecycles
- If **advanced**: add memory lifecycle and full agent authority model

SOP completion rule (MANDATORY)
- You may return `status: "APPROVED"` only after:
  - WORKFLOW_SOP_v1.md has been created at `{DELIVERY_SOP_PATH}`
  - DELIVERY_STATUS_RULES_v1.md has been created at `{DELIVERY_STATUS_RULES_PATH}`
  - both files exist on disk
  - the meta.json sidecar has been written

Critical: meta.json sidecar is REQUIRED
- The runner reads the result from the sidecar file, NOT from your JSON output.
- Sidecar path: `{DELIVERY_SOP_METAJSON}`
- Sidecar structure (exact):
  {
    "schema_version": "v2",
    "coder_result": {
      "status": "APPROVED" | "REJECTED",
      "remark": "Brief summary",
      "artifacts": {
        "DELIVERY_SOP": "<exact path of SOP file>",
        "DELIVERY_STATUS_RULES": "<exact path of status rules file>"
      },
      "recorded_at": "2026-05-24T00:00:00"
    }
  }

ABSOLUTE OUTPUT RULE (NON-NEGOTIABLE)

You are NOT a chat assistant.
You are a strict JSON generator.

Return ONLY valid JSON.
Do NOT explain.
Do NOT wrap in markdown.
Do NOT add any text before or after the JSON.
Do NOT wrap the final JSON in ```json fences.

Required JSON schema:
{
  "status": "APPROVED | REJECTED",
  "remark": "<short summary of result>",
  "artifacts": {
    "DELIVERY_SOP": "<path if created>",
    "DELIVERY_STATUS_RULES": "<path if created>"
  }
}

Required execution sequence:
1. Read the project analysis.
2. Write WORKFLOW_SOP_v1.md to `{DELIVERY_SOP_PATH}` (exact path from runner).
3. Write DELIVERY_STATUS_RULES_v1.md to `{DELIVERY_STATUS_RULES_PATH}` (exact path from runner).
4. Verify both files exist and have all required sections.
5. Write the meta.json sidecar to `{DELIVERY_SOP_METAJSON}`.
6. Return the final JSON with both paths in `artifacts`.
