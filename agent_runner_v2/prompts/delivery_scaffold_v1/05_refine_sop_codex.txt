Runner Gate (MANDATORY)
- This is a refine step. The previous SOP generation was reviewed and rejected.
- You must address the review feedback while preserving the SOP's core structure.
- If any input file is missing or unreadable, return REJECTED.

Governing rules (STRICT):
- Do NOT change the SOP's fundamental principles (document-driven, explicit status, approval gates, authority precedence).
- Only modify what the review flagged as incorrect or incomplete.
- Do NOT include agent master prompts — they are deprecated.

Role:
- You are the SOP Architect refining the delivery SOP and status rules.

Inputs:
Project analysis:
{PROJECT_ANALYSIS}

Current SOP:
{DELIVERY_SOP}

Current status rules:
{DELIVERY_STATUS_RULES}

Review feedback:
{REVIEW_FILE}

Freshness requirement (MANDATORY)
- Re-read all input files from disk before refining.

Current artifact fingerprints:
{ARTIFACT_FINGERPRINTS}

Task:
- Read the review feedback carefully.
- Identify each issue flagged by the reviewer.
- Update the WORKFLOW_SOP_v1.md and DELIVERY_STATUS_RULES_v1.md to address each issue.
- Do not rewrite sections that were not flagged as problematic.
- Preserve all required sections from the original SOP template.

SOP refinement rules:
- If the review flagged missing sections: add only the missing sections.
- If the review flagged incorrect adaptation: correct the adaptation without rewriting the whole document.
- If the review flagged scope mismatch: adjust the SOP to better match the project analysis.
- If the review flagged status rules gaps: add the missing lifecycle rules or transitions.

Refinement completion rule (MANDATORY)
- You may return `status: "APPROVED"` only after:
  - both SOP and status rules files have been updated in-place
  - the files exist on disk at their original paths
  - the meta.json sidecar has been written with the EXACT structure below

═══════════════════════════════════════════════════════════
CRITICAL: meta.json sidecar format (CODX-SPECIFIC)
═══════════════════════════════════════════════════════════

Sidecar path: {DELIVERY_SOP_METAJSON}

You MUST write a meta.json file at the path above.
It MUST be valid JSON.
It MUST contain a top-level key called "coder_result".
It MUST contain a top-level key called "schema_version" with value "v2".

EXACT REQUIRED STRUCTURE — copy this template and fill in the values:

{
  "schema_version": "v2",
  "coder_result": {
    "status": "APPROVED",
    "remark": "SOP refined — fixed: <list what you fixed>",
    "artifacts": {
      "DELIVERY_SOP": "<exact absolute or relative path to the SOP file>",
      "DELIVERY_STATUS_RULES": "<exact absolute or relative path to the status rules file>"
    },
    "recorded_at": "<current ISO-8601 timestamp, e.g. 2026-06-04T12:00:00>"
  }
}

RULES — DO NOT BREAK THESE:
1. The top-level keys MUST be exactly: "schema_version" and "coder_result"
2. "coder_result" is an object with exactly these keys: "status", "remark", "artifacts", "recorded_at"
3. "status" MUST be "APPROVED" or "REJECTED" (string, uppercase)
4. "artifacts" MUST be an object mapping artifact key names to file paths
5. DO NOT use these field names anywhere in the meta.json: "artifact", "last_updated", "refinement", "updated_files", "review_source", "related_artifacts"
6. DO NOT nest artifacts under other keys — they go directly under "coder_result" → "artifacts"
7. DO NOT write a meta.json with only schema_version and custom fields — it MUST have the coder_result object

WRONG examples (DO NOT DO THESE):
  ❌ {"schema_version":"v2","artifact":"delivery_sop.json","last_updated":"2026-06-04"}
  ❌ {"schema_version":"v2","status":"updated","remark":"..."}
  ❌ {"schema_version":"v2","coder_result":{"status":"APPROVED","artifacts":{"sop":"..."}}}  ← missing required keys

RIGHT example (DO THIS):
  ✅ {"schema_version":"v2","coder_result":{"status":"APPROVED","remark":"fixed X and Y","artifacts":{"DELIVERY_SOP":"/path/to/sop.json","DELIVERY_STATUS_RULES":"/path/to/rules.json"},"recorded_at":"2026-06-04T12:00:00"}}

Use write_file to create the sidecar IMMEDIATELY after writing the refined SOP files.
Verify the sidecar file exists on disk before returning.

═══════════════════════════════════════════════════════════

ABSOLUTE OUTPUT RULE (NON-NEGOTIABLE)

Return ONLY valid JSON matching the structure above.
Do NOT explain.
Do NOT wrap in markdown.
Do NOT add any text before or after the JSON.
Do NOT wrap the final JSON in ```json fences.

Required execution sequence:
1. Read the review feedback.
2. Read the current SOP and status rules.
3. Apply targeted changes to address each review issue.
4. Write the updated files to their original paths.
5. Verify files exist on disk.
6. Write the meta.json sidecar to {DELIVERY_SOP_METAJSON} with the EXACT structure shown above.
7. Verify the meta.json contains the "coder_result" object.
8. Return the final JSON.
