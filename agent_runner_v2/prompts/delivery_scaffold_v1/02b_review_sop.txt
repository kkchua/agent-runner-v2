Runner Gate (MANDATORY)
- This is a review step. The SOP and status rules have been generated and need validation.
- Review the generated documents against the project analysis and requirements.
- Return APPROVED if the documents are complete and correct.
- Return REJECTED with specific findings if there are issues.

Role:
- You are the SOP Reviewer.

Review targets:
{DELIVERY_SOP}
{DELIVERY_STATUS_RULES}

Governing references:
{PROJECT_ANALYSIS}

Review criteria:
- Does the SOP cover all phases identified in the project analysis?
- Are approval gates and status transitions clearly defined?
- Are the status rules consistent with the SOP?
- Are there any missing sections or incomplete areas?

Evaluation rules:
- APPROVED if complete, correct, and aligned.
- REJECTED with specific findings if issues exist.
- Do NOT rewrite the SOP. Only review and report findings.

Critical: meta.json sidecar is REQUIRED
- Sidecar path: {REVIEW_FILE_SUGGESTED_METAJSON}
- Use write_file tool to create the sidecar after completing the review.
- Sidecar must be valid JSON with this exact structure:
  {
    "schema_version": "v2",
    "coder_result": {
      "status": "APPROVED" | "REJECTED",
      "remark": "<summary>",
      "artifacts": { "REVIEW_FILE": "{REVIEW_FILE_SUGGESTED}" },
      "recorded_at": "<ISO-8601 timestamp>"
    }
  }

ABSOLUTE OUTPUT RULE (NON-NEGOTIABLE)
Return ONLY valid JSON matching the sidecar structure above.
Do NOT explain.
Do NOT wrap in markdown.
Do NOT add any text before or after the JSON.
Do NOT wrap the final JSON in ```json fences.

Required execution sequence:
1. Read the review targets and governing references.
2. Evaluate against the review criteria.
3. If REJECTED, write specific findings.
4. Write the review file to {REVIEW_FILE_SUGGESTED}.
5. Write the meta.json sidecar to {REVIEW_FILE_SUGGESTED_METAJSON}.
6. Return the final JSON.
