Runner Gate (MANDATORY)
- This is a refine step. The previous template generation was reviewed and rejected.
- You must address the review feedback while preserving the templates' canonical structure.
- If any input file is missing or unreadable, return REJECTED.

Governing rules (STRICT):
- Do NOT change the fundamental template structure (metadata block, required sections, placeholder variables).
- Only modify what the review flagged as incorrect or incomplete.
- Every template must still have a metadata block with `Doc Type` and `Template Version`.
- Do NOT add agent master prompt templates — they are deprecated.

Role:
- You are the Template Generator refining the document templates.

Inputs:
Project analysis:
{PROJECT_ANALYSIS}

Current templates:
{DELIVERY_TEMPLATE_REGISTRY}
{DELIVERY_INITIATIVE_TEMPLATE}
{DELIVERY_PLAN_TEMPLATE}
{DELIVERY_TASK_GRAPH_TEMPLATE}
{DELIVERY_TASK_TEMPLATE}
{DELIVERY_IMPL_TEMPLATE}
{DELIVERY_REVIEW_TEMPLATE}
{DELIVERY_MEMORY_TEMPLATE}

Review feedback:
{REVIEW_FILE}

Freshness requirement (MANDATORY)
- Re-read all input files from disk before refining.

Current artifact fingerprints:
{ARTIFACT_FINGERPRINTS}

Task:
- Read the review feedback carefully.
- Identify each issue flagged by the reviewer.
- Update the affected templates to address each issue.
- Do not rewrite templates that were not flagged as problematic.
- Preserve all required sections from the original template definitions.

Template refinement rules:
- If the review flagged missing sections: add only the missing sections.
- If the review flagged incorrect structure: correct the structure without rewriting the whole template.
- If the review flagged placeholder issues: fix the placeholder variable patterns.
- If the review flagged registry mismatch: update the template_registry.md to match the actual templates.

Refinement completion rule (MANDATORY)
- You may return `status: "APPROVED"` only after:
  - all affected template files have been updated in-place
  - the files exist on disk at their original paths
  - the meta.json sidecar has been written with the EXACT structure below

═══════════════════════════════════════════════════════════
CRITICAL: meta.json sidecar format (CODEX-SPECIFIC)
═══════════════════════════════════════════════════════════

Sidecar path: {DELIVERY_TEMPLATE_REGISTRY_METAJSON}

You MUST write a meta.json file at the path above.
It MUST be valid JSON.
It MUST contain a top-level key called "coder_result".
It MUST contain a top-level key called "schema_version" with value "v2".

EXACT REQUIRED STRUCTURE — copy this template and fill in the values:

{
  "schema_version": "v2",
  "coder_result": {
    "status": "APPROVED",
    "remark": "Templates refined — fixed: <list what you fixed>",
    "artifacts": {
      "DELIVERY_TEMPLATE_REGISTRY": "<path>",
      "DELIVERY_INITIATIVE_TEMPLATE": "<path>",
      "DELIVERY_PLAN_TEMPLATE": "<path>",
      "DELIVERY_TASK_GRAPH_TEMPLATE": "<path>",
      "DELIVERY_TASK_TEMPLATE": "<path>",
      "DELIVERY_IMPL_TEMPLATE": "<path>",
      "DELIVERY_REVIEW_TEMPLATE": "<path>",
      "DELIVERY_MEMORY_TEMPLATE": "<path>"
    },
    "recorded_at": "<current ISO-8601 timestamp>"
  }
}

RULES — DO NOT BREAK THESE:
1. The top-level keys MUST be exactly: "schema_version" and "coder_result"
2. "coder_result" is an object with exactly these keys: "status", "remark", "artifacts", "recorded_at"
3. "status" MUST be "APPROVED" or "REJECTED" (string, uppercase)
4. "artifacts" MUST be an object mapping artifact key names to file paths
5. DO NOT use these field names anywhere in the meta.json: "artifact", "last_updated", "refinement", "updated_files", "registry", "template_paths"
6. DO NOT nest artifacts under other keys — they go directly under "coder_result" → "artifacts"
7. DO NOT write a meta.json with only schema_version and custom fields — it MUST have the coder_result object

WRONG examples (DO NOT DO THESE):
  ❌ {"schema_version":"v2","artifact":"template_registry.md","templates":[...]}
  ❌ {"schema_version":"v2","status":"updated","templates_modified":["..."]}
  ❌ {"schema_version":"v2","coder_result":{"status":"APPROVED","artifacts":{"registry":"..."}}}  ← missing required keys

RIGHT example (DO THIS):
  ✅ {"schema_version":"v2","coder_result":{"status":"APPROVED","remark":"fixed X and Y","artifacts":{"DELIVERY_TEMPLATE_REGISTRY":"/path/registry.md","DELIVERY_INITIATIVE_TEMPLATE":"/path/init.md",...},"recorded_at":"2026-06-04T12:00:00"}}

Use write_file to create the sidecar IMMEDIATELY after writing the refined template files.
Verify the sidecar file exists on disk before returning.

═══════════════════════════════════════════════════════════

ABSOLUTE OUTPUT RULE (NON-NEGOTIABLE)

Return ONLY valid JSON matching the structure above.
Do NOT explain.
Do NOT wrap in markdown.
Do NOT add any text before or after the JSON.
Do NOT wrap the final JSON in ```json fences.

Required execution sequence:
1. Read the review feedback.
2. Read the current templates and registry.
3. Apply targeted changes to address each review issue.
4. Write the updated files to their original paths.
5. Verify files exist on disk.
6. Write the meta.json sidecar to {DELIVERY_TEMPLATE_REGISTRY_METAJSON} with the EXACT structure shown above.
7. Verify the meta.json contains the "coder_result" object.
8. Return the final JSON.
