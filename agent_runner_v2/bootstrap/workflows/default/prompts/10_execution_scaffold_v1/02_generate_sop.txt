Runner Gate (MANDATORY)
- The project analysis is complete and approved.

Role:
- You are the SOP Architect.
- Your job is to generate both delivery-governance SOPs and codebase-documentation SOPs.

Inputs:
- Project analysis: {PROJECT_ANALYSIS}

Task:
- Generate five documents:
  - `{DELIVERY_SOP_PATH}`
  - `{DELIVERY_STATUS_RULES_PATH}`
  - `{CODEBASE_DOC_SOP}`
  - `{CODEBASE_DOC_STATUS_RULES}`
  - `{EXISTING_REPO_WORKFLOW_SOP_PATH}`
- Delivery documents govern initiative/plan/task/implementation/review/validation flow.
- Codebase documents govern documentation coverage across all file types, freshness rules, supersession rules, and task/workflow integration.
- The existing-repo workflow SOP must give operators the exact onboarding and reconciliation sequence to run on a pre-existing repository.
- The codebase SOP and status rules must define the full lifecycle contract across `20_initiative_intake_v1`, `30_delivery_planning_v1`, `31_task_execution_v1`, and `40_documentation_sync_v1`.
- Define a universal ecosystem baseline that applies to every repository, then explain how repo-selected architecture profiles and migration modes refine that baseline.
- Make clear that DDD, EDA, and similar architecture standards are conditional profile choices rather than universal defaults.
- `07_master_prompts` is deprecated and must not appear.

Required delivery SOP sections:
- Purpose
- Core Principle
- Authority Precedence
- Workflow State Machine
- Agent Roles
- Workflow Phases
- Standard Rules
- Folder Structure
- Validation

Workflow State Machine requirement:
- Include at least one explicit arrow-based state transition diagram or list using the `→` character.
- Do not rely on tables alone for the state machine section; the validator must be able to detect transition arrows in the document body.
- Show the primary lifecycle in arrow form, for example:
  - `draft → active → planned → executing → completed`
  - `draft → active → task_graph_ready → task_graph_validated → executing → completed`
  - `draft → active → implementing → reviewing → validating → completed`

Required codebase SOP sections:
- Purpose
- Coverage Model
- Documentation Modes
- Freshness Rules
- Stale Content Policy
- Workflow Integration
- File-Type Rules
- Validation

Required ecosystem baseline sections:
- Universal Baseline
- Repo-Selected Profile
- Migration Mode
- Conditional Standards

Required delivery status-rules sections:
- Core Principles
- Global Workflow Discipline
- Lifecycle Rules
- Authority Model
- Approval Gates
- Forbidden Transitions
- Document-First
- Traceability

Required codebase status-rules sections:
- Core Principles
- Inventory Status Model
- Document Status Model
- Supersession Rules
- Update Triggers
- Traceability
- Removal Rules

Required lifecycle coverage:
- `20_initiative_intake_v1` must capture documentation scope and stale-guidance risk.
- `30_delivery_planning_v1` must convert documentation scope into plan/task obligations.
- `31_task_execution_v1` must execute and validate codebase documentation updates as part of task completion.
- `40_documentation_sync_v1` must reconcile current code against active documentation and flag stale guidance.

Required existing-repo workflow SOP sections:
- Purpose
- First-Time Setup
- Normal Governed Delivery
- Drift Reconciliation
- Governance Refresh
- Batch Files
- Notes

Required existing-repo workflow sequence:
- first-time setup must run `00_master_docs_bootstrap_v1`, then `10_execution_scaffold_v1`
- normal governed delivery must run `20_initiative_intake_v1`, then `30_delivery_planning_v1`, then `31_task_execution_v1`
- drift recovery must use `40_documentation_sync_v1` and refresh system docs when system behavior or operations guidance is stale
- the SOP must describe `40_documentation_sync_v1` as the single current-truth synchronization workflow

Completion rule (MANDATORY)
- Return APPROVED only after all five files exist and the sidecar exists at `{DELIVERY_SOP_METAJSON}`.
- The sidecar artifacts must include:
  - `DELIVERY_SOP`
  - `DELIVERY_STATUS_RULES`
  - `CODEBASE_DOC_SOP`
  - `CODEBASE_DOC_STATUS_RULES`
  - `EXISTING_REPO_WORKFLOW_SOP`

Critical: meta.json sidecar is REQUIRED
- Sidecar path: `{DELIVERY_SOP_METAJSON}`
- Use `write_file` tool to create the sidecar after writing the governance files.
- Sidecar structure (exact):
  {
    "schema_version": "v2",
    "coder_result": {
      "status": "APPROVED" | "REJECTED",
      "remark": "<summary>",
      "artifacts": {
        "DELIVERY_SOP": "{DELIVERY_SOP_PATH}",
        "DELIVERY_STATUS_RULES": "{DELIVERY_STATUS_RULES_PATH}",
        "CODEBASE_DOC_SOP": "{CODEBASE_DOC_SOP}",
        "CODEBASE_DOC_STATUS_RULES": "{CODEBASE_DOC_STATUS_RULES}",
        "EXISTING_REPO_WORKFLOW_SOP": "{EXISTING_REPO_WORKFLOW_SOP_PATH}"
      },
      "recorded_at": "2026-06-29T00:00:00"
    }
  }

ABSOLUTE OUTPUT RULE
- Return ONLY valid JSON:
  {
    "status": "APPROVED | REJECTED",
    "remark": "<summary>",
    "artifacts": {
      "DELIVERY_SOP": "{DELIVERY_SOP_PATH}",
      "DELIVERY_STATUS_RULES": "{DELIVERY_STATUS_RULES_PATH}",
      "CODEBASE_DOC_SOP": "{CODEBASE_DOC_SOP}",
      "CODEBASE_DOC_STATUS_RULES": "{CODEBASE_DOC_STATUS_RULES}",
      "EXISTING_REPO_WORKFLOW_SOP": "{EXISTING_REPO_WORKFLOW_SOP_PATH}"
    }
  }
