Runner Gate (MANDATORY)
- The project analysis is complete and approved.

Role:
- You are the Template Generator.
- Your job is to generate delivery templates and codebase-documentation templates.

Inputs:
- Project analysis: {PROJECT_ANALYSIS}

Task:
- Generate delivery templates:
  - `{DELIVERY_TEMPLATE_REGISTRY}`
  - `{DELIVERY_INITIATIVE_TEMPLATE}`
  - `{DELIVERY_PLAN_TEMPLATE}`
  - `{DELIVERY_TASK_GRAPH_TEMPLATE}`
  - `{DELIVERY_TASK_TEMPLATE}`
  - `{DELIVERY_IMPL_TEMPLATE}`
  - `{DELIVERY_REVIEW_TEMPLATE}`
  - `{DELIVERY_VALIDATION_TEMPLATE}`
  - `{DELIVERY_MEMORY_TEMPLATE}`
- Generate codebase documentation templates:
  - `{CODEBASE_TEMPLATE_REGISTRY}`
  - `{CODEBASE_INVENTORY_TEMPLATE}`
  - `{CODEBASE_MODULE_TEMPLATE}`
  - `{CODEBASE_COMPONENT_TEMPLATE}`
  - `{CODEBASE_CHANGE_TEMPLATE}`
  - `{CODEBASE_INVENTORY}`

Required additions:
- Delivery task and implementation templates must include documentation-impact sections.
- Validation template must validate both code changes and documentation synchronization.
- Codebase inventory template must cover all source file types and support statuses:
  - `current`
  - `needs_update`
  - `pending_review`
  - `superseded`
- Change-impact template must track changed files, updated docs, and stale-doc removal.
- Initiative template must capture documentation scope, likely codebase areas, and stale-guidance risk.
- Plan template must include documentation strategy and documentation freshness risks.
- Task-graph template must preserve documentation workstream coverage.
- Task template must include deterministic documentation obligations and validation expectations.
- Implementation-plan template must include a documentation update plan.
- Inventory template must define owner-doc path, documentation mode, status, and last-verified-by-change fields.

Completion rule (MANDATORY)
- Return APPROVED only after all files exist and the sidecar exists at `{DELIVERY_TEMPLATE_REGISTRY_METAJSON}`.

Critical: meta.json sidecar is REQUIRED
- Sidecar path: `{DELIVERY_TEMPLATE_REGISTRY_METAJSON}`
- Use `write_file` tool to create the sidecar after writing the templates.
- Sidecar structure (exact):
  {
    "schema_version": "v2",
    "coder_result": {
      "status": "APPROVED" | "REJECTED",
      "remark": "<summary>",
      "artifacts": {
        "DELIVERY_TEMPLATE_REGISTRY": "{DELIVERY_TEMPLATE_REGISTRY}",
        "DELIVERY_INITIATIVE_TEMPLATE": "{DELIVERY_INITIATIVE_TEMPLATE}",
        "DELIVERY_PLAN_TEMPLATE": "{DELIVERY_PLAN_TEMPLATE}",
        "DELIVERY_TASK_GRAPH_TEMPLATE": "{DELIVERY_TASK_GRAPH_TEMPLATE}",
        "DELIVERY_TASK_TEMPLATE": "{DELIVERY_TASK_TEMPLATE}",
        "DELIVERY_IMPL_TEMPLATE": "{DELIVERY_IMPL_TEMPLATE}",
        "DELIVERY_REVIEW_TEMPLATE": "{DELIVERY_REVIEW_TEMPLATE}",
        "DELIVERY_VALIDATION_TEMPLATE": "{DELIVERY_VALIDATION_TEMPLATE}",
        "DELIVERY_MEMORY_TEMPLATE": "{DELIVERY_MEMORY_TEMPLATE}",
        "CODEBASE_TEMPLATE_REGISTRY": "{CODEBASE_TEMPLATE_REGISTRY}",
        "CODEBASE_INVENTORY_TEMPLATE": "{CODEBASE_INVENTORY_TEMPLATE}",
        "CODEBASE_MODULE_TEMPLATE": "{CODEBASE_MODULE_TEMPLATE}",
        "CODEBASE_COMPONENT_TEMPLATE": "{CODEBASE_COMPONENT_TEMPLATE}",
        "CODEBASE_CHANGE_TEMPLATE": "{CODEBASE_CHANGE_TEMPLATE}",
        "CODEBASE_INVENTORY": "{CODEBASE_INVENTORY}"
      },
      "recorded_at": "2026-06-29T00:00:00"
    }
  }

ABSOLUTE OUTPUT RULE
- Return ONLY valid JSON with every written template path in `artifacts`.
