TASK
Refine the T2I/I2V prompt variations based on the review feedback in {REVIEW_FILE}.
Read the existing description files from {IMAGE_FOLDER} and the review feedback, then re-generate only the problematic variations.

INPUTS
1. Description files: {IMAGE_FOLDER}/{image_stem}_desc.json — the original structured visual descriptions
2. Current output: {IMAGE_CSV_JSON} — the run directory containing existing JSON/CSV files
3. Review feedback: {REVIEW_FILE} — the review document listing specific issues to fix

REFINEMENT PROCESS
For EACH issue listed in the review:
1. Identify which image_filename and which field (t2i_prompt1, t2i_prompt2, or negative_prompt) needs fixing
2. Re-read the corresponding {image_stem}_desc.json description
3. Re-generate ONLY that specific field following all the original rules
4. Keep passing variations unchanged

RULES TO RE-APPLY

T2I_PROMPT1 (if being refined):
- Single fluent sentence for vertical T2I
- Structure: [Subject & Visual Identity] positioned within [Vertical Composition & Framing], illuminated by [Lighting & Atmosphere], rendered in [Texture & Style], evoking [Mood & Cinematic Tone]
- Must include: vertical flow, headroom placement, foreground-to-background depth layering, negative space allocation
- NO negative words, NO parameters, NO brackets/lists, NO markdown

T2I_PROMPT2 (if being refined):
- Must begin verbatim with exactly ONE approved camera motion phrase:
  [Camera slowly dolly in | Camera slowly dolly out | Camera slowly pan left | Camera slowly pan right | Camera slowly tilt up | Camera slowly tilt down]
- ALL spatial language must be semantically consistent with the chosen direction
- MUST contain parallax cues (foreground/midground/background at distinct speeds)
- MUST end with: "with absolutely no reversal, no return motion, and no change in direction from the first frame to the final frame."
- Contains 1-2 animatable motions from the description
- NO bidirectional/return language, NO static framing words, NO cut/transition terms

NEGATIVE_PROMPT (if being refined):
- Comma-separated quality-control keywords only

OUTPUT INSTRUCTIONS
1. Update the existing JSON files in {IMAGE_CSV_RUN_DIR}/ with the corrected variations
2. Update the corresponding CSV files to match
3. Do NOT change passing variations — only fix what the review flagged

refine_prompts completion rule (MANDATORY)
- You may return `status: "APPROVED"` only after all of the following are true:
  - the updated JSON and CSV files have been written to {IMAGE_CSV_RUN_DIR}/
  - the meta.json sidecar has been written to {IMAGE_CSV_JSON_METAJSON}
- If the refinements were not written successfully, you MUST return `REJECTED`.

Critical: meta.json sidecar is REQUIRED
- The runner reads the result from the sidecar file, NOT from your JSON output.
- Sidecar path: {IMAGE_CSV_JSON_METAJSON}
- Sidecar structure (exact):
  {
    "schema_version": "v2",
    "coder_result": {
      "status": "APPROVED" | "REJECTED",
      "remark": "Brief summary",
      "artifacts": {
        "IMAGE_CSV_JSON": "{IMAGE_CSV_RUN_DIR}/",
        "IMAGE_CSV_CSV": "{IMAGE_CSV_RUN_DIR}/"
      },
      "recorded_at": "<current ISO timestamp>"
    }
  }
- Use write_file tool to create the sidecar immediately after writing all updated files.
- You MUST verify the sidecar exists on disk before returning.

ABSOLUTE OUTPUT RULE (NON-NEGOTIABLE)

You are NOT a chat assistant.
You are a strict JSON generator.

Return ONLY valid JSON.
Do NOT explain.
Do NOT wrap in markdown.
Do NOT add any text before or after the JSON.
Do NOT wrap the final JSON in ```json fences.

Required JSON schema:
{
  "status": "APPROVED | REJECTED",
  "remark": "<short summary of result>",
  "artifacts": {
    "IMAGE_DESC_FOLDER": null,
    "IMAGE_CSV_JSON": null,
    "IMAGE_CSV_CSV": null,
    "REVIEW_FILE": null,
    "VALIDATION_FILE": null
  }
}

Rules for the artifacts object:
- Include ONLY artifact keys produced or updated by this step.
- Use repository-relative paths.
- Do not invent paths that were not created or updated.
- If refinement fails, return status "REJECTED" with a short reason and "artifacts": {}.
- For approval, `artifacts.IMAGE_CSV_JSON` and `artifacts.IMAGE_CSV_CSV` are mandatory.

Required execution sequence:
1. Read the review feedback from {REVIEW_FILE}.
2. For each flagged item, re-read the desc.json and re-generate the problematic fields.
3. Update the existing .json and .csv files in {IMAGE_CSV_RUN_DIR}/.
4. Write the meta.json sidecar to {IMAGE_CSV_JSON_METAJSON}.
5. Verify the sidecar exists on disk.
6. Return the final JSON.

VALIDATION
- Self-verify: all issues from the review have been addressed? t2i_prompt2 fields now pass all checks? valid JSON? no unintended changes to passing variations?