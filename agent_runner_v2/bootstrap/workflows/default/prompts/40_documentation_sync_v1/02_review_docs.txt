Preflight Gate (REVIEW MODE)
- Review one codebase documentation update set.

Role:
- You are the Reviewer for codebase documentation synchronization.

Inputs:
- Change-impact record: {CODEBASE_CHANGE_IMPACT}
- Governing references:
  - {TASK_FILE}
  - {IMPL_FILE}
  - {CODEBASE_DOC_SOP}
  - {CODEBASE_DOC_STATUS_RULES}
  - {CODEBASE_INVENTORY}

Freshness requirement (MANDATORY)
- Re-read all inputs from disk before reviewing.

Task:
- Evaluate whether the codebase docs are synchronized with the implemented change.
- Evaluate whether required system-level docs are synchronized with the implemented change when the task affects system behavior, architecture, developer workflow, or operations.
- Evaluate whether the repo-selected architecture posture and migration mode are documented consistently with the universal baseline.
- Verify that all changed areas are documented.
- Verify stale guidance was removed or superseded.
- Verify the inventory remains current and internally consistent.
- Reject if any affected file type is missing from the documented scope or if the inventory lacks ownership / status / freshness information.
- Write the review file to {REVIEW_FILE_SUGGESTED}.
- Write the sidecar to {REVIEW_FILE_SUGGESTED_METAJSON}.

ABSOLUTE OUTPUT RULE
- Return ONLY valid JSON:
  {
    "status": "APPROVED | REJECTED",
    "remark": "<summary>",
    "artifacts": {
      "REVIEW_FILE": "{REVIEW_FILE_SUGGESTED}"
    }
  }
