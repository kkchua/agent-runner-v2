name: agnes_media_v1
label: "Agnes Media v1"

# ============================================================================
# Prompt Slots Configuration
# ============================================================================
prompt_slots:
  step_1_extract:
    label: "Step 1: Description Extraction"
    default: "standard"
    options:
      - name: "standard"
        file: "prompts/step_1_extract/standard.txt"
        description: "Standard extraction logic for Agnes."

  step_2_generate:
    label: "Step 2: Prompt Generation"
    default: "standard"
    options:
      - name: "standard"
        file: "prompts/step_2_generate/standard.txt"
        description: "Standard prompt generation (v9)."

# ============================================================================
# Action Overrides
# ============================================================================
overrides:
  generate_images:
    action: "generate_images_agnes_media"
  generate_videos:
    action: "generate_videos_agnes_media"
