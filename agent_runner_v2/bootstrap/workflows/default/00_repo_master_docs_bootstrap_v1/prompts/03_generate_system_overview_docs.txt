You are generating the core repo governance document set for the current repository.

This step builds on the approved repo governance analysis. Keep the scope limited to repo-governance documents, not codebase implementation documentation.

Inputs you must read first:
- `{PROJECT_ANALYSIS}`
- `{CODEBASE_DOC_ROOT}/01_inventory/codebase_inventory.md`
- `{CODEBASE_DOC_ROOT}/04_changes/` latest change impact document

Repository evidence boundary:
- use only current-repository files as evidence for repo scope, ownership, architecture posture, and operational expectations
- ignore global runner-home content such as `.ukbe-runner/`
- ignore packaged bootstrap workflow copies shipped with the shared runner
- do not promote workflow metadata or prompt paths outside the current repository root into repo facts

Exclude from evidence:
- root guidance files `CLAUDE.md`, `QWEN.md`, and `AGENTS.md`
- generated guidance copies under Layer 1 bootstrap outputs
- do not use those files to infer active repo governance scope, migration scope, or documentation ownership

Write or refresh these docs:
- `{SYSTEM_DOCS_INDEX}`
- `{SYSTEM_DOC_STANDARD}`

Primary objectives:
1. Define the repo-governance document set and its audience.
2. Define the repo documentation standard and ownership expectations.
3. Keep the governance contract minimal and separate from codebase implementation documentation.
4. Make the document index point readers to repo governance docs and repo codebase evidence without duplicating either.
5. Keep all markdown output ASCII-only.

Frontmatter contract:
- EVERY file MUST include these YAML frontmatter fields:
  - `template_id`
  - `version: "1.0.0"`
  - `doc_type: "system"`
  - `managed_by: "workflow-generated"`
  - `generated_at: <current ISO timestamp>`
  - `workflow: "00_repo_master_docs_bootstrap_v1"`
  - `step: <current step name>`
  - `change_id: <job id from context>`

Use these exact `template_id` values:
- `README.md` -> `SYS-00-IDX`
- `REPO_DOCUMENTATION_STANDARD.md` -> `SYS-00-DS`

Required content rules:
- `{SYSTEM_DOCS_INDEX}` should index the repo governance set and describe audience views.
- `{SYSTEM_DOC_STANDARD}` should define repo documentation rules, governance boundaries, update triggers, and validation expectations.
- `{SYSTEM_DOCS_INDEX}` should link to `PROJECT_ANALYSIS.md`, `REPO_DOCUMENTATION_STANDARD.md`, `DECISION_LOG.md`, and `DEVELOPER_GUIDE.md`.
- `{SYSTEM_DOCS_INDEX}` may reference `docs/repo/codebase/` as the implementation evidence area, but must not restate codebase details.
- `{SYSTEM_DOC_STANDARD}` should define the governance doc boundary and explicitly state that implementation structure, runtime behavior, modules, and operational mechanics belong under `docs/repo/codebase/`.
- When referring to the repo-level documentation standard, use the exact filename `REPO_DOCUMENTATION_STANDARD.md`.
- Reserve `DOCUMENTATION_STANDARD.md` for Layer 1 references only.
- Do not use non-ASCII punctuation or mojibake characters anywhere in the markdown output.
- Do not describe workflow/runtime mechanics such as sidecars, daemon loops, subprocess sequencing, polling, heartbeats, or prompt/coder internals in these repo-governance docs.

Use these section headings exactly:
- `README.md`: `System Documentation Index`, `Audience Views`, `Document Map`
- `REPO_DOCUMENTATION_STANDARD.md`: `Purpose`, `Audience Model`, `Document Set`, `Architecture Baseline`, `Repository-Selected Profile`, `Migration Mode`, `Conditional Standards`, `Update Triggers`, `Validation`

Artifact reporting rule:
- In your result sidecar and final JSON, report canonical artifact keys, not filenames.
- Report `SYSTEM_DOCS_INDEX` for `README.md`.
- Report `SYSTEM_DOC_STANDARD` for `REPO_DOCUMENTATION_STANDARD.md`.
- Do not invent filename-based artifact keys such as `REPO_DOCUMENTATION_STANDARD`.
