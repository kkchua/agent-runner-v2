You are generating the remaining repo governance companion docs for the current repository.

This step completes the governance set. Keep the result grounded in repo evidence, but do not turn codebase implementation analysis into governance content.

Inputs you must read first:
- `{PROJECT_ANALYSIS}`
- `{SYSTEM_DOCS_INDEX}`
- `{SYSTEM_DOC_STANDARD}`
- `{CODEBASE_DOC_ROOT}/01_inventory/codebase_inventory.md`
- `{CODEBASE_DOC_ROOT}/04_changes/` latest change impact document

Repository evidence boundary:
- treat only the current repository tree as authoritative implementation evidence
- ignore global runner-home content such as `.ukbe-runner/`
- ignore packaged bootstrap workflow copies shipped with the shared runner
- if a workflow, launcher, prompt path, or runtime asset is not present under the current repository root, do not document it as repo-owned behavior

Write or refresh these docs:
- `{DECISION_LOG}`
- `{DEVELOPER_GUIDE}`
- `{SYSTEM_DOCS_CHANGE_LOG}`

Primary objectives:
1. Record current governance decisions and assumptions.
2. Give developers a governance-facing guide to working in this repository.
3. Summarize what this bootstrap pass refreshed and what still needs follow-up.
4. Keep all markdown output ASCII-only.

Required content rules:
- `{DECISION_LOG}` should summarize current decisions, rationale, and follow-up decisions at governance level.
- `{DEVELOPER_GUIDE}` should focus on developer-facing repo guidance: how docs are split, where codebase evidence lives, where repo governance lives, and how to keep them aligned.
- `{DEVELOPER_GUIDE}` must not restate architecture-flow, failure-mode, or module-internal content that belongs in `docs/repo/codebase/`.
- `{SYSTEM_DOCS_CHANGE_LOG}` should summarize refreshed governance docs, key findings, and remaining follow-up items.
- Do not create or refresh runbooks, workflow SOPs, system context docs, component architecture docs, integration maps, failure modes docs, or architecture-flow docs in this step.
- Do not use non-ASCII punctuation or mojibake characters anywhere in the markdown output.
- Keep workflow/runtime mechanics such as sidecars, daemon loops, heartbeats, polling, subprocess sequencing, and prompt/coder internals out of `DEVELOPER_GUIDE.md`.

Frontmatter contract:
- `{DECISION_LOG}` -> `template_id: "SYS-03-DL"`
- `{DEVELOPER_GUIDE}` -> `template_id: "ENG-01-DG"`
- every generated markdown file must include the standard workflow-generated frontmatter used by this workflow

Use these section headings exactly:
- `DECISION_LOG.md`: `Decision Table`, `Follow-Up Decisions`
- `DEVELOPER_GUIDE.md`: `Development Workflow`, `Key Commands`, `Documentation Responsibilities`, `Architecture Posture`
