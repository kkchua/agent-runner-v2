# Objective

Produce the Artifact Contract for the target workflow. Define all
artifact keys, filename patterns, and descriptions.

# Reference Inputs

- WORKFLOW_SPEC_FILE: {WORKFLOW_SPEC_FILE}
- TEST_CRITERIA_FILE: {TEST_CRITERIA_FILE}
- DOMAIN_ANALYSIS_FILE: {DOMAIN_ANALYSIS_FILE}
- DOMAIN_COMPONENT_SCHEMA_FILE: {DOMAIN_COMPONENT_SCHEMA_FILE}
- COMPOSITION_FORMAT_FILE: {COMPOSITION_FORMAT_FILE}
- OUTPUT_FORMAT_FILE: {OUTPUT_FORMAT_FILE}

# Generation Tasks

1. **Input Artifacts** -- WORKFLOW_SPEC_FILE, BASE_COMPOSITION_STANDARD
2. **Per-Phase Output Artifacts** -- One key per phase output
3. **Cross-Phase Artifacts** -- TEST_CRITERIA_FILE, review files
4. **Package Artifacts** -- workflow.toml, context_extensions.py,
   actions.py, prompts/, README.md, Standards/, Specs/
5. **Filename Patterns** -- Each key maps to a pattern with {job_id}, {seq}
6. **Conflict Check** -- Verify no collisions with global registry
7. **Naming Conventions** -- UPPER_SNAKE_CASE, _FILE suffix

# Forbidden Content

- Do NOT use builder-specific artifact paths
- Do NOT include non-ASCII characters

# Output Instructions

Write to: {ARTIFACT_CONTRACT_FILE}
Markdown with YAML frontmatter: doc_type: "artifact_contract",
identity_locked: true. ASCII-only.
