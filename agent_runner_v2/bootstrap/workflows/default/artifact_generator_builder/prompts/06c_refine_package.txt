# Objective

Refine the generated workflow package based on review feedback.

# Reference Inputs

- REVIEW_PACKAGE_FILE: {REVIEW_PACKAGE_FILE}
- WORKFLOW_MANIFEST_FILE: {WORKFLOW_MANIFEST_FILE}

# Refinement Tasks

1. **Address Review Feedback** -- For each issue:
   - Fix identity leakage
   - Add missing files
   - Correct artifact bindings
   - Fix routing issues
   - Improve prompts

2. **Regenerate Affected Files** -- Update only what's needed:
   - If workflow.toml has issues, regenerate it
   - If prompts have issues, regenerate them
   - If context_extensions.py has issues, regenerate it

3. **Self-Validation** -- Verify all review feedback addressed

# Self-Critic

- Did I address ALL review feedback?
- Is the package now complete and compliant?
- Is there any builder identity leakage?

# Forbidden Content

- Do NOT ignore any review feedback
- Do NOT introduce builder identity
- Do NOT include non-ASCII characters

# Output Instructions

Write to: {WORKFLOW_MANIFEST_FILE} (and other affected files)
Overwrite previous versions. ASCII-only for all files.
