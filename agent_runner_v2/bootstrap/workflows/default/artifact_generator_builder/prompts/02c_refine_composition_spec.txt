# Objective

Refine the composition specification based on review feedback. Address all identified issues.

# Reference Inputs

- REVIEW_COMPOSITION_SPEC_FILE: {REVIEW_COMPOSITION_SPEC_FILE}
- COMPOSITION_SPEC_FILE: {COMPOSITION_SPEC_FILE}

# Refinement Tasks

1. **Address Review Feedback** -- For each issue:
   - Fix gaps in meta schema
   - Clarify input/output mappings
   - Improve transformation rules
   - Enhance extension mechanism

2. **Enhance Clarity** -- Improve implementability:
   - Add examples where helpful
   - Clarify ambiguous rules
   - Make contracts more specific

3. **Self-Validation** -- Verify all review feedback addressed

# Self-Critic

- Did I address ALL review feedback?
- Is the spec now complete and consistent?
- Can a runtime implementation be designed from this?

# Forbidden Content

- Do NOT ignore any review feedback
- Do NOT introduce new issues
- Do NOT include non-ASCII characters

# Output Instructions

Write to: {COMPOSITION_SPEC_FILE} (overwrite previous version)
Markdown with YAML frontmatter: doc_type: "composition_spec", identity_locked: true. ASCII-only.
