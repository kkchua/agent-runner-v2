# Objective

Design the default runtime implementation that executes the composition spec to produce output from input.

# MANDATORY: BASE_COMPOSITION_STANDARD_v1.0.md Section 10

You MUST follow BASE_COMPOSITION_STANDARD_v1.0.md Section 10 ({BASE_COMPOSITION_STANDARD}). This section defines:
- The DEFAULT_IMPL_FILE deliverable (Section 10.1) -- you MUST produce it as a self-contained document
- The codename rules -- you MUST use "{CODENAME}" as the identity, NOT generate your own

Failure to produce the DEFAULT_IMPL_FILE deliverable with correct naming is a REJECT.

# Reference Inputs

- COMPOSITION_SPEC_FILE: {COMPOSITION_SPEC_FILE}
- REQUIREMENT_ANALYSIS_FILE: {REQUIREMENT_ANALYSIS_FILE}
- REQUIREMENT_DOC: {REQUIREMENT_DOC}
- BASE_COMPOSITION_STANDARD: {BASE_COMPOSITION_STANDARD}
- CODENAME: {CODENAME}

# Generation Tasks

Produce a runtime implementation design containing:

1. **Implementation Architecture** -- High-level structure, component modules, data flow
2. **Input Loading** -- How to load, parse, and validate input
3. **Transformation Engine** -- Step-by-step processing, how composition spec rules are applied, error handling
4. **Output Generation** -- How to produce ALL output artifacts defined in the requirement doc
5. **Configuration** -- Runtime parameters, defaults, overrides
6. **Extension Interface** -- How to add new implementations

# Output Instructions

Write TWO files:

1. {RUNTIME_IMPL_FILE} -- Intermediate design notes (doc_type: "runtime_impl")
2. {DEFAULT_IMPL_FILE} -- Default runtime implementation deliverable (doc_type: "default_impl")

The DEFAULT_IMPL_FILE is a self-contained deliverable per BASE_COMPOSITION_STANDARD_v1.0.md Section 10.1. It must include complete algorithm descriptions, data structures, configuration defaults, and extension point implementations.

Both files: identity_locked: true, generator_name: "{CODENAME}", codename: "{CODENAME}". ASCII-only.

# Forbidden Content

- Do NOT reference the builder's identity
- Do NOT deviate from the composition spec
- Do NOT include non-ASCII characters
