# Objective

Review the generated workflow package and all deliverables for completeness, correctness, and compliance with design artifacts.

# MANDATORY: BASE_COMPOSITION_STANDARD_v1.0.md Section 10

You MUST verify compliance with BASE_COMPOSITION_STANDARD_v1.0.md Section 10 ({BASE_COMPOSITION_STANDARD}). REJECT if:
- Any of the four required deliverables is missing (Section 10.1)
- File names do not match the required patterns (Section 10.2)
- Codename "{CODENAME}" is not used consistently
- Builder identity is referenced anywhere

# Reference Inputs

- WORKFLOW_MANIFEST_FILE: {WORKFLOW_MANIFEST_FILE}
- STEP_SEQUENCE_FILE: {STEP_SEQUENCE_FILE}
- COMPOSITION_STANDARD_FILE: {COMPOSITION_STANDARD_FILE}
- CODENAME: {CODENAME}

# Review Tasks

1. **Workflow Package** -- workflow.toml steps/routing match design, artifact bindings match contract, context_extensions.py valid, actions.py complete, prompts correct, README accurate
2. **Identity Isolation** -- All files use {CODENAME}, zero builder identity leakage
3. **Composition Standard Deliverable** -- Adapts base standard for this domain. Uses codename "{CODENAME}"
4. **Input Contract Compliance** (BASE_COMPOSITION_STANDARD Section 6.5):
   - All input document artifact keys in workflow.toml use `_FILE` suffix
   - context_extensions.py registers input artifacts with `_FILE` suffix
   - Prompt placeholders reference the correct `_FILE` keys
5. **Output Delivery Contract** (BASE_COMPOSITION_STANDARD Section 6.6):
   - Output artifacts declare delivery locations in context_extensions.py
   - Workflow includes a delivery/promotion step
   - README documents output destination
6. **Runtime Implementation Model** (BASE_COMPOSITION_STANDARD Section 13.8):
   - File structure includes `impls/default/` folder with default.impl.md
   - Default implementation maps all steps to concrete prompts/actions
   - Shared actions.py and prompts/ exist at workflow root
   - Composition standard defines abstract step interfaces

# Output Instructions

Write to: {REVIEW_PACKAGE_FILE}
Markdown with YAML frontmatter: doc_type: "review_package", verdict: "PASS" or "REJECT", identity_locked: true. ASCII-only.

# Forbidden Content

- Do NOT approve if builder identity leaked
- Do NOT approve if files are missing
- Do NOT include non-ASCII characters
