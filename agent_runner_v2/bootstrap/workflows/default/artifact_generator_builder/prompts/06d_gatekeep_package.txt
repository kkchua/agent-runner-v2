# Objective

Final gatekeep of the generated workflow package and all deliverables. Approve only if complete, correct, and ready for promotion.

# MANDATORY: BASE_COMPOSITION_STANDARD_v1.0.md Section 10

You MUST verify compliance with BASE_COMPOSITION_STANDARD_v1.0.md Section 10 ({BASE_COMPOSITION_STANDARD}). REJECT if:
- Any of the four required deliverables is missing (Section 10.1)
- File names or directory structure do not match (Section 10.2)
- Codename "{CODENAME}" is not used consistently
- Builder identity is referenced anywhere

# Reference Inputs

- WORKFLOW_MANIFEST_FILE: {WORKFLOW_MANIFEST_FILE}
- COMPOSITION_STANDARD_FILE: {COMPOSITION_STANDARD_FILE}
- REVIEW_PACKAGE_FILE: {REVIEW_PACKAGE_FILE}

# Gatekeep Tasks

1. **Completeness** -- All files present: workflow.toml, context_extensions.py, actions.py, prompts/, README.md, composition standard, `impls/default/` folder
2. **Correctness** -- Routing valid, artifact keys match, Python/TOML syntax valid
3. **Identity Isolation** -- No builder leakage, target codename "{CODENAME}" used throughout
4. **Deliverable Quality** -- Composition standard is self-contained and valid
5. **Review Feedback Resolution** -- All issues addressed
6. **Input Contract** (BASE_COMPOSITION_STANDARD Section 6.5):
   - All input document artifact keys use `_FILE` suffix in workflow.toml and context_extensions.py
7. **Output Delivery Contract** (BASE_COMPOSITION_STANDARD Section 6.6):
   - Output delivery location declared, delivery step present
8. **Runtime Implementation Model** (BASE_COMPOSITION_STANDARD Section 13.8):
   - `impls/default/default.impl.md` exists with component mapping for all steps
   - Shared actions.py and prompts/ at workflow root
   - Composition standard defines abstract step interfaces

# Output Instructions

Write to: {GATEKEEP_PACKAGE_FILE}
Markdown with YAML frontmatter: doc_type: "gatekeep_package", verdict: "APPROVE" or "REJECT", identity_locked: true. ASCII-only.

# Forbidden Content

- Do NOT approve if critical issues remain
- Do NOT include non-ASCII characters
