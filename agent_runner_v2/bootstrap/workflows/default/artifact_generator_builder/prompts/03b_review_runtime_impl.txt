# Objective

Review the runtime implementation design and default implementation deliverable for correctness, completeness, and alignment with the composition spec.

# MANDATORY: BASE_COMPOSITION_STANDARD_v1.0.md Section 10

You MUST verify compliance with BASE_COMPOSITION_STANDARD_v1.0.md Section 10 ({BASE_COMPOSITION_STANDARD}). REJECT if:
- DEFAULT_IMPL_FILE is missing or not self-contained
- Codename "{CODENAME}" is not used for identity
- Builder identity is referenced anywhere

# Reference Inputs

- RUNTIME_IMPL_FILE: {RUNTIME_IMPL_FILE}
- DEFAULT_IMPL_FILE: {DEFAULT_IMPL_FILE}
- COMPOSITION_SPEC_FILE: {COMPOSITION_SPEC_FILE}
- CODENAME: {CODENAME}

# Review Tasks

1. **Spec Compliance** -- Implementation follows composition spec (input mapping, transformation rules, output mapping, extension mechanism)
2. **Completeness** -- All aspects covered (architecture, input loading, transformation engine, output generation, configuration, extension interface)
3. **Feasibility** -- Algorithms are implementable, error handling is adequate
4. **Default Impl Deliverable** -- DEFAULT_IMPL_FILE is self-contained, covers all transformation stages, uses codename "{CODENAME}"
5. **Impl Component Mapping** (BASE_COMPOSITION_STANDARD Section 13.8):
   - Default implementation provides a component mapping for ALL abstract step interfaces
   - Each step is mapped to a specific prompt template or action function
   - The mapping declares which components are shared vs impl-specific
   - The impl folder structure follows `impls/default/` convention
6. **Shared vs Impl-Specific Architecture** (BASE_COMPOSITION_STANDARD Section 13.8):
   - Shared actions.py and prompts/ are identified at workflow root level
   - Impl-specific overrides are placed under `impls/default/`
   - The design supports adding new implementations by creating new `impls/{name}/` folders

# Output Instructions

Write to: {REVIEW_RUNTIME_IMPL_FILE}
Markdown with YAML frontmatter: doc_type: "review_runtime_impl", verdict: "PASS" or "REJECT", identity_locked: true. ASCII-only.

# Forbidden Content

- Do NOT approve if spec compliance issues exist
- Do NOT invent new requirements
- Do NOT include non-ASCII characters
