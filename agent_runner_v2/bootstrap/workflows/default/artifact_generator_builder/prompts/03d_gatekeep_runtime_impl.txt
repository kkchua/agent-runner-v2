# Objective

Final gatekeep of the runtime implementation design and default implementation deliverable. Approve only if complete, compliant, and implementable.

# Reference Inputs

- RUNTIME_IMPL_FILE: {RUNTIME_IMPL_FILE}
- DEFAULT_IMPL_FILE: {DEFAULT_IMPL_FILE}
- REVIEW_RUNTIME_IMPL_FILE: {REVIEW_RUNTIME_IMPL_FILE}
- BASE_COMPOSITION_STANDARD: {BASE_COMPOSITION_STANDARD}

# Gatekeep Tasks

1. Spec compliance -- follows composition spec
2. Completeness -- all aspects covered
3. Feasibility -- can be implemented
4. Default impl deliverable -- self-contained and valid
5. Review feedback resolution -- all issues addressed
6. **Impl Component Mapping** (BASE_COMPOSITION_STANDARD Section 13.8):
   - Default implementation maps ALL abstract step interfaces to concrete components
   - Each step is assigned a specific prompt template or action function
   - Shared vs impl-specific components are clearly identified
   - Impl folder structure follows `impls/default/` convention

# Output Instructions

Write to: {GATEKEEP_RUNTIME_IMPL_FILE}
Markdown with YAML frontmatter: doc_type: "gatekeep_runtime_impl", verdict: "APPROVE" or "REJECT", identity_locked: true. ASCII-only.

# Forbidden Content

- Do NOT approve if critical issues remain
- Do NOT include non-ASCII characters
