You are generating the reusable Layer 1 ecosystem governance master docs.

This workflow is intentionally narrow. It owns only the permanent Layer 1
governance docs. It must remain reusable across ecosystems and repositories.

Read first:
- relevant source files under `agent_runner_v2/`
- shared registry files under `workflows/_registry/`
- any existing docs under `{SYSTEM_DOC_ROOT}/`

Write or refresh only:
- `{SYSTEM_DOCS_INDEX}`
- `{SYSTEM_DOC_STANDARD}`
- `{BUNDLE_TAXONOMY}`
- `{RUNTIME_GOVERNANCE}`

Hard ownership rules:
- Do not write outside the four files listed above.
- Do not generate repo-local analysis, SDLC docs, bundle-local SOP docs,
  audience docs, or delivery runtime artifacts.
- Do not define a repository's concrete workflow inventory.
- Do not define a repository's concrete scaffold workflow name.
- Do not define provider authentication flows.
- Use ASCII characters only in generated markdown body text. Do not emit
  smart quotes, em dashes, en dashes, replacement characters, or other
  non-ASCII punctuation.

Layering model to preserve:
1. Layer 1 = ecosystem governance
2. Layer 2 = repo or bundle master-doc and operating structure
3. Layer 3 = plugin workflow families and repo-local outputs

Layer 1 must define:
- documentation authority
- documentation structure rules
- bundle class and ownership boundaries
- runtime publish/install/control-plane expectations
- validation gates
- the generic contract for plugin workflow bundles
- the rule that workflow bundles own workflow-specific artifact path contracts

Layer 1 must not define:
- concrete workflow IDs
- concrete plugin bundle names
- repo-derived artifact names
- repo-derived filenames
- repo-specific output examples
- any repo-local fallback resolution policy for loading workflow bundles

Strict scope exclusions:
- Do not include any concrete workflow identifier matching a pattern like
  `00_example_workflow_v1` anywhere in document body text.
- Concrete workflow identifiers are allowed only inside the required YAML
  frontmatter and the workflow-managed protection banner immediately after
  frontmatter.
- Do not include the literal text `{ARTIFACT_KEY_` anywhere.
- Do not mention `delivery_scaffold_v1` anywhere.
- Do not enumerate repo-derived artifacts such as `PROJECT_ANALYSIS`,
  `CODEBASE_INVENTORY`, `SYSTEM_OVERVIEW`, `FUNCTIONAL_SPEC`,
  `SYSTEM_CONTEXT`, `COMPONENT_ARCHITECTURE`, `DEVELOPER_GUIDE`, or
  `RUNBOOK`.

Required frontmatter:
- `template_id`
- `version: "1.0.0"`
- `doc_type: "system"`
- `managed_by: "workflow-generated"`
- `generated_at`
- `workflow: "00_layer1_governance_bootstrap_v1"`
- `step: "generate_layer1_governance_docs"`
- `change_id`

Template IDs:
- `README.md` -> `SYS-00-IDX`
- `DOCUMENTATION_STANDARD.md` -> `SYS-00-DS`
- `BUNDLE_TAXONOMY.md` -> `SYS-00-BT`
- `RUNTIME_GOVERNANCE.md` -> `SYS-00-RG`

Required sections:
- `README.md`
  - `System Documentation Index`
  - `Audience Views`
  - `Document Map`
- `DOCUMENTATION_STANDARD.md`
  - `Purpose`
  - `Audience Model`
  - `Document Set`
  - `Architecture Baseline`
  - `Conditional Standards`
  - `Update Triggers`
  - `Validation`
- `BUNDLE_TAXONOMY.md`
  - `Bundle Classes`
  - `Ownership Rules`
  - `Packaging Rules`
- `RUNTIME_GOVERNANCE.md`
  - `Purpose`
  - `Runtime Scope Model`
  - `Bundle Publish And Install Model`
  - `Registry Control Plane`
  - `Plugin Bundle Control Model`
  - `Role And Connection Resolution`
  - `Artifact Ownership Enforcement`
  - `Execution Mode Parity`
  - `Validation Gates`
  - `Change Control`

Document-specific rules:
- `README.md` must explain the Layer 1 / Layer 2 / Layer 3 model without
  naming any concrete workflow inventory.
- `README.md` must explicitly state that repo-local outputs live under
  `docs/repo/` and are outside Layer 1 ownership.
- `DOCUMENTATION_STANDARD.md` must define rules only for the four Layer 1
  docs.
- `RUNTIME_GOVERNANCE.md` must define the steady-state runtime model and must
  explicitly state that plugin workflow bundles may be either
  `single-workflow` bundles or `multi-workflow` bundles.
- `RUNTIME_GOVERNANCE.md` must explicitly state that shared runtime code may
  provide only generic path helpers and enforcement, while workflow bundles
  own workflow-specific document output paths, review/validation path rules,
  and generated/protected document inventories.
- `RUNTIME_GOVERNANCE.md` must define the global published bundle copy under
  the global runtime home as the canonical runtime source.
- `RUNTIME_GOVERNANCE.md` must not define repo-local workflow fallback,
  dual-path discovery, or local repository fallback when a global bundle is
  missing.
- `RUNTIME_GOVERNANCE.md` must not discuss concrete repo-local output paths,
  `docs/repo/`, or ownership of repository-specific outputs. Keep it at
  runtime control-plane, registry, publish/install, validation, and parity
  rules level only.
- `RUNTIME_GOVERNANCE.md` must explicitly reject workflow-name-specific path
  resolution branches and centralized workflow-family path registries as the
  steady-state ownership model.

## BUNDLE_TAXONOMY.md Constraints (CRITICAL - MOST REFINEMENTS FAIL HERE)

BUNDLE_TAXONOMY.md is ONLY about bundle classification and ownership.
It must NOT drift into runtime behavior or path resolution.

### BUNDLE_TAXONOMY.md MUST contain:
- Bundle class definitions (Core Governance, Plugin Workflow, Domain)
- Ownership rules for each bundle class
- Packaging rules for each bundle class
- Bundle-owned responsibility for workflow-specific artifact path contracts

### BUNDLE_TAXONOMY.md MUST NOT contain (CRITICAL):
- Runtime path resolution policy (global-first, local-fallback, dual-path)
- Any mention of `docs/repo/` or repo-local output paths
- Repository-specific output ownership discussion
- How bundles are discovered or loaded at runtime

If you find yourself writing about where bundles are loaded from,
where they are resolved, or fallback behavior, STOP and DELETE that content.
BUNDLE_TAXONOMY.md is about WHAT bundles ARE, not HOW they are LOADED.

## Pre-flight Self-Check (MANDATORY)

Before writing any file, state these checks explicitly:

1. [ ] BUNDLE_TAXONOMY.md contains NO runtime path resolution policy
2. [ ] BUNDLE_TAXONOMY.md contains NO `docs/repo/` mention
3. [ ] RUNTIME_GOVERNANCE.md contains NO `docs/repo/` mention
4. [ ] RUNTIME_GOVERNANCE.md contains NO dual-path or local-fallback definitions
5. [ ] Only README.md mentions `docs/repo/`
6. [ ] No concrete workflow identifiers in body text (frontmatter/banner only)
7. [ ] RUNTIME_GOVERNANCE.md uses phrases `single-workflow` AND `multi-workflow`
8. [ ] Layer 1 explicitly states that workflow bundles own workflow-specific artifact path contracts
9. [ ] Layer 1 explicitly rejects workflow-name-specific path resolution branches in shared runtime

After writing files, re-read from disk and verify each check passes.

Before returning final JSON:
- complete all four files
- re-read all four files from disk
- verify every required section exists
- verify no file contains a concrete workflow identifier in body text beyond
  the required frontmatter and workflow-managed protection banner
- verify no file contains `{ARTIFACT_KEY_`
- verify no file contains `delivery_scaffold_v1`
- verify `README.md` explicitly mentions `docs/repo/` as the repo-local
  output boundary outside Layer 1 ownership
- verify `DOCUMENTATION_STANDARD.md` contains no repo-derived artifact names
- verify `BUNDLE_TAXONOMY.md` contains no `docs/repo/` reference and no
  repository-output ownership discussion
- verify `RUNTIME_GOVERNANCE.md` contains no `docs/repo/` reference and no
  repository-output ownership discussion
- verify no file contains mojibake markers or replacement-character corruption
- verify all four files are ASCII-only
- verify `RUNTIME_GOVERNANCE.md` explicitly uses the phrases
  `single-workflow` and `multi-workflow`
- verify `RUNTIME_GOVERNANCE.md` explicitly states that workflow bundles own
  workflow-specific artifact path contracts
- verify `RUNTIME_GOVERNANCE.md` explicitly rejects workflow-name-specific
  path resolution branches and centralized workflow-family path registries as
  the steady-state model
- verify `RUNTIME_GOVERNANCE.md` does not mention dual-path discovery, local
  repository fallback, or fallback loading from the repo workflows directory
- verify `BUNDLE_TAXONOMY.md` does not define runtime path resolution policy
- verify the task is incomplete until `{RUNTIME_GOVERNANCE}` exists on disk
  and the required meta.json sidecar has been written
